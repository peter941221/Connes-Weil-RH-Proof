# Unconditional RH Completion Plan

Date: 2026-07-01

Status: current local implementation roadmap. This file is not public proof
material.

Target:

```lean
theorem unconditional_rh : _root_.RiemannHypothesis := ...
```

The target theorem must have no parameters. It must not hide a
`RouteCertificate`, a `SourceObjectPackage`, source-model law fields, loose
bridge `Prop` fields, reviewer decisions, or placeholder theorem assumptions.

## Current Boundary

The repository still does not prove `_root_.RiemannHypothesis`
unconditionally.

Evidence:

| object | evidence |
|---|---|
| final endpoint remains certificate-conditional | `ConnesWeilRH/Route/RouteTheorem.lean:2577`, `final_connes_weil_rh {inputs} (cert : RouteCertificate inputs)` |
| route certificate still packages proof data | `ConnesWeilRH/Route/RouteTheorem.lean:24`, `RouteCertificate` |
| source object package still owns cross-object assumptions | `ConnesWeilRH/Source/Objects.lean:257`, `SourceObjectPackage` |
| normalized support-square comparison remains a record input on the route path | `ConnesWeilRH/Route/TraceFrontEnd.lean:2360`, `NormalizedSupportSquareQWLambdaSourceComparison` |
| no-extra-bulk is explicit but still a contract input | `ConnesWeilRH/Route/TraceFrontEnd.lean:837`, `TraceScaleNoExtraBulkContract` |
| no certified no-argument theorem is imported | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:4349`, `unconditional_rh_contract_skeleton` exists only in the quarantined Dev skeleton |
| normalized no-argument outlet is package-closed but not proof-closed | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:4206`, `NormalizedNoArgumentRouteCertificatePackage`; `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:4217`, `normalizedNoArgumentRouteCertificatePackageFromTheorems` |
| remaining `sorry`s are quarantined | 26 `sorry`s in `ConnesWeilRH/Dev/UnconditionalSkeleton.lean`, from line 56 through line 1721 |

Current proof graph:

```text
source theorem data / accepted imports / concrete Lean proofs
        |
        v
SourceObjectPackage
        |
        v
ExpandedSourceFixedSTestFrontEnd
        |
        v
TraceFrontEndData / ExpandedSourceTraceReadOffFrontEnd
        |
        v
TraceScaleRouteFrontEndData
        |
        v
RouteCertificate
        |
        v
final_connes_weil_rh
        |
        v
_root_.RiemannHypothesis

Dev skeleton outlet:

NormalizedNoArgumentRouteCertificatePackage
        |
        v
unconditional_rh_contract_skeleton
        |
        v
_root_.RiemannHypothesis

This second path is not certification. It packages the normalized route
assembly behind quarantined theorem inputs.
```

## Latest 2026-07-01 Alignment

The latest `MEMORY.md` entry is ahead of the older S2-B1-focused roadmap
language below.  Current local progress closed the CC20 member of the three
core source-model bodies: `normalizedCoreCC20TraceModelFromTheorems` is now
definitionally the trace model generated by
`Source.CC20Concrete.TraceScale.normalizedLegalSquareTraceScaleToCC20TraceModel`
from `normalizedCoreS2B1NormalizedSeedFromTheorems`.  The five CC20 trace-model
rows then project from concrete TraceScale theorems instead of remaining as
separate row holes.

Evidence:

| item | evidence | status |
|---|---|---|
| CC20 source-model body now comes from the normalized seed | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:230`, `normalizedCoreS2B1NormalizedSeedFromTheorems`; `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:234`, `normalizedCoreCC20TraceModelFromTheorems` | Green relative to the seed input |
| CC20 trace-model equality is definitional | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:245`, `normalizedCoreCC20TraceModel_eq_normalizedSeed` | Green |
| concrete TraceScale-to-CC20 trace model projection exists | `ConnesWeilRH/Source/CC20Concrete/TraceScale.lean:1071`, `cc20_trace_model_of_normalized_seed_trace_object`; `ConnesWeilRH/Source/CC20TraceModel.lean:80`, `cc20_trace_model_of_trace_object_symbols` | Green projection |
| CCM24 source-model rows remain theorem inputs | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:54`, semilocal symbols; lines 58, 63, 68, 73 for canonical/support/bounded/Sonin rows | Red/Orange |
| CCM25 source-model rows remain theorem inputs | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:113`, Weil symbols; lines 117, 122, 134, 139, 144 for `QW`, `Psi`, `QW_lambda`, finite-prime, and pole rows | Red/Orange |
| current quarantined proof-hole count | `rg -n "\bsorry\b" ConnesWeilRH/Dev/UnconditionalSkeleton.lean` returns 26 matches | Skeleton |

Current distance in one sentence:

```text
Route endpoint:
  Green inside Dev

Certified source content:
  still blocked by 26 quarantined theorem inputs
```

`MEMORY.md` now records the first six-package attack list as route-engineering
closed, with theorem inputs still explicit.  Lean first closed the
support-square/`QW_lambda` row for a matched normalized square seed whose trace
amplitude is chosen from the CCM25 restricted evaluator.  The active Source
lane then closed the actual source-seed scalar identification through
`normalizedSeedQWLambdaScalarIdentificationOfTheoremBasePackage` and wired the
normalized trace consumer through that theorem.

The later 2026-07-01 entries closed the remaining-five-row S2-B1 row package,
the normalized source/fixed/trace front-end package, the ledger/sign-defect
package, the restricted-to-full threshold package, and the final C.1/RH
definition exit package.  These are Green package/reducer closures.  They do
not supply the accepted-source or concrete Lean proof inputs needed to build a
no-argument `RouteCertificate`.  The latest S2-B1 core pass also removed the
early six-row theorem-data black box by assembling it from the already derived
support-square/`QW_lambda` read-off plus one five-row constructor-input
package.

The newest entry closes item 6 one layer further: the normalized Dev lane now
has a no-argument package that stores the route certificate, final-exit
package, and projected Mathlib RH result together.  That package does not
remove the theorem inputs above it.  It only proves that, if the quarantined
normalized inputs are supplied, the final no-argument consumer has no more
route-endpoint plumbing to do.

The latest S2-B1 finite-part pass narrows the no-hidden finite-part row.  The
row no longer comes from one opaque constructor input.  It now passes through a
source scalar object with three named scalars and two exact equalities:
`actualFinitePart = normalizedFinitePart` and
`subtractedFinitePartTerm = 0`.  This is Green interface work, not analytic
discharge.  The next proof target is those two scalar equalities for the exact
fixed tuple.

Evidence:

| item | evidence | status |
|---|---|---|
| source module is in the active import path | `ConnesWeilRH.lean:25`, `import ConnesWeilRH.Source.S2B1TraceScale` | Green |
| fixed-tuple S2-B1 theorem data is named and row-shaped | `ConnesWeilRH/Source/S2B1TraceScale.lean:36`, `S2B1FixedTupleTheoremData` | Green interface |
| concrete CC20 support-square read-off is definitional for the normalized square seed | `ConnesWeilRH/Source/S2B1TraceScale.lean:185`, `normalized_seed_cc20_support_square_trace_read_off` | Green |
| CCM25 restricted evaluator read-off comes from the concrete package | `ConnesWeilRH/Source/S2B1TraceScale.lean:273`, `normalized_seed_ccm25_qw_lambda_source_evaluator_read_off` | Green relative to package |
| matched seed sets trace amplitude to `sqrt` of the CCM25 restricted evaluator | `ConnesWeilRH/Source/S2B1TraceScale.lean:297`, `normalizedSeedMatchedToCCM25Evaluator` | Green construction |
| matched amplitude square equals the evaluator under nonnegativity | `ConnesWeilRH/Source/S2B1TraceScale.lean:311`, `normalized_seed_matched_trace_amplitude_sq_eq_ccm25_evaluator` | Green conditional |
| matched seed support-square/`QW_lambda` source data is closed | `ConnesWeilRH/Source/S2B1TraceScale.lean:379`, `normalizedSeedMatchedSupportSquareQWLambdaReadOff` | Green conditional |
| matched seed support-square row can now enter fixed-tuple theorem data while the other five S2-B1 rows remain explicit inputs | `ConnesWeilRH/Source/S2B1TraceScale.lean:417`, `matchedSeedFixedTupleTheoremDataOfRemainingRows` | Green conditional first-row packaging |
| actual normalized-seed scalar identity is now proved from the theorem-base support-square row | `ConnesWeilRH/Source/S2B1TraceScale.lean:539`, `NormalizedSeedQWLambdaScalarIdentification`; `ConnesWeilRH/Source/S2B1TraceScale.lean:575`, `normalizedSeedQWLambdaScalarIdentificationOfTheoremBasePackage` | Green active Source theorem |
| evaluator-level actual scalar identity still projects to the package-free target | `ConnesWeilRH/Source/S2B1TraceScale.lean:557`, `normalizedSeedQWLambdaScalarIdentificationOfRestrictedEvaluator` | Green bridge |
| package-free actual scalar identity builds the first fixed-tuple row and theorem data while the other five S2-B1 rows remain explicit | `ConnesWeilRH/Source/S2B1TraceScale.lean:597`, `normalizedSeedSupportSquareQWLambdaReadOffOfQWLambdaScalarIdentification`; `ConnesWeilRH/Source/S2B1TraceScale.lean:744`, `normalizedSeedFixedTupleTheoremDataOfQWLambdaScalarIdentification` | Green bridge over Green Source theorem |
| remaining-five-row S2-B1 fixed-tuple package is named and active | `S2B1FixedTupleRemainingRowsPackage`, `ofTheoremData`, `ofConstructorInputs`, `toAnalyticExclusionPackage` | Green source row package |
| early core six-row theorem-data input is no longer an atom | `S2B1FixedTupleRemainingConstructorInputPackage`, `S2B1TraceScaleTheoremData.ofSupportSquareAndRemainingConstructorInputs`, `normalizedCoreS2B1RemainingConstructorInputPackageFromTheorems`, `normalizedCoreS2B1TraceScaleTheoremDataInputFromTheorems` | Green assembly; five source-row inputs remain |
| normalized trace tuple now consumes the remaining-five-row package before deriving no-extra-bulk rows | `normalizedTraceFixedTupleRemainingRowsPackageFromTheorems`, `normalizedTraceFixedTupleAnalyticExclusionFromTheorems`, `normalizedTraceNoExtraBulkTermInputFromTheorems`, `normalizedTraceNoHiddenFinitePartInputFromTheorems` | Green wiring, skeleton theorem inputs remain |
| normalized trace fixed-tuple support-square row consumes the proved scalar identity | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:1418`, `normalizedActualScalarIdentificationFromTheoremBasePackage`; `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:2034`, `normalizedTraceFixedTupleSupportSquareQWLambdaRowFromTheorems` | Green normalized trace consumer; core six-row theorem-data input split |
| no-hidden finite-part row is scalar-normal-form shaped | `S2B1FinitePartSourceScalarData`, `S2B1FinitePartSourceNormalFormData`, `S2B1FinitePartNormalFormRows.toNoHiddenFinitePartRows`, `S2B1NoHiddenFinitePartSubtractionConstructorRows.toConstructorInput` | Green interface; two equality theorems remain |
| normalized scalar trace lane consumes a front-end package | `normalizedScalarFrontEndPackageFromTheorems`, `normalizedScalarFrontEndPackageSourceTraceReadOffData_eq`, `normalizedScalarTraceFrontEndFromTheorems`, `normalizedScalarTraceAmplitudeSquareFromTheorems` | Green scalar front-end package consumption; theorem inputs remain explicit |
| normalized no-argument Dev outlet is closed | `NormalizedNoArgumentRouteCertificatePackage`, `normalizedNoArgumentRouteCertificatePackageFromTheorems`, `mathlib_rh_of_normalized_no_argument_route_certificate_package`, `unconditional_rh_contract_skeleton` | Green Dev package consumption; not certification |

Boundary:

```text
What Lean now proves:
  matched seed amplitude := sqrt(CCM25 restricted evaluator)
        |
        v
  supportSquareTrace = QW_lambda

What the active Source theorem now proves:
  actual/source CC20 trace amplitude^2
        =
  QW_lambda for the same fixed tuple
```

This now produces a no-argument RH theorem only inside the quarantined Dev
skeleton. It does not produce a certified project theorem. The early Dev
skeleton input `normalizedCoreS2B1ActualScalarIdentificationFromTheorems`
remains a quarantined theorem input, and the early core path now has one
quarantined five-row constructor-input package. The six-row S2-B1 theorem-data
input is no longer a separate atom.

## What Changed

The old 15-item checklist overstated the current remaining work. Several broad
inputs have been narrowed or wired through active Lean declarations.

Current completed route-engineering progress:

| area | current status | evidence |
|---|---|---|
| active route `sorry`s | none in active import path | placeholder scan reports only `ConnesWeilRH/Dev/UnconditionalSkeleton.lean` |
| active source S2-B1 module | imported by `ConnesWeilRH` | `ConnesWeilRH.lean:25`, `ConnesWeilRH.Source.S2B1TraceScale` |
| normalized no-defect target | active theorem bodies exist | `normalized_source_no_defect_reduces_to_package_formula`, `normalized_source_no_defect_reduces_to_normal_form` |
| CC20 normalized square scalar block | definitional theorems exist | `normalized_seed_cc20_support_square_trace_read_off`, `normalized_seed_cc20_source_no_defect_trace_read_off`, `normalized_seed_cc20_positive_trace_read_off` |
| CCM25 restricted evaluator read-off | package-backed theorem exists | `normalized_seed_ccm25_qw_lambda_source_evaluator_read_off` |
| support-square comparison target | active Source theorem proves the actual CC20 source seed scalar identity by chaining the CC20 support-square read-off with the theorem-base `supportSquareTrace = QW_lambda` row; the normalized trace fixed-tuple support-square row consumes that theorem | `normalizedSeedQWLambdaScalarIdentificationOfTheoremBasePackage`, `normalizedActualScalarIdentificationFromTheoremBasePackage`, `normalizedTraceFixedTupleSupportSquareQWLambdaRowFromTheorems` |
| remaining five S2-B1 fixed-tuple rows | active source row package closes the row block after the support-square scalar leg; normalized no-missing-bulk now consumes this package before deriving positive-decomposition, no-extra-bulk, and no-hidden-finite-part rows; early core theorem data now consumes a five-row constructor-input package instead of assuming all six rows at once | `S2B1FixedTupleRemainingRowsPackage`, `S2B1FixedTupleRemainingConstructorInputPackage`, `normalizedTraceFixedTupleRemainingRowsPackageFromTheorems`, `normalizedCoreS2B1RemainingConstructorInputPackageFromTheorems` |
| rank/zero-mode target | current normalized route tuple is proved from sign/defect Row 5 rank-ledger identification | `S2B1RankZeroModeChannelClassified`, `rankZeroModeChannelClassifiedAtHolds_of_rank_pole_identification`, `rankZeroModeChannelClassifiedAtHolds_of_sign_defect_classification` |
| no-hidden finite-part target | source scalar object and normal-form rows exist; the analytic content is now exactly two scalar equalities | `S2B1FinitePartSourceScalarData`, `S2B1FinitePartSourceNormalFormData`, `finitePartNormalizationFixedStatement`, `noSubtractedFinitePartTermStatement` |
| scalar full-trace balance | pole/archimedean half now reduces to restricted-to-full evidence | `normalizedScalarFullTraceArchimedeanPoleBalanceOfQWLambdaRestriction`, `normalizedScalarFullTraceArchimedeanPoleBalanceOfRestrictedToFullContract` |
| fixed-tuple S2-B1 row packaging | six fixed-tuple rows now assemble from exact theorem-base term-ledger rows before producing the no-bulk witness | `S2B1TraceScaleFixedTupleAnalyticExclusionPackage`, `SourceObjectTheoremBasePackage.s2b1FixedTupleAnalyticExclusion`, `SourceObjectTheoremBasePackage.s2b1FixedTupleNoBulkWitness` |
| no-extra-bulk | source-term statement now names decomposition, no bulk term, and no hidden finite-part subtraction | `TraceScaleNoExtraBulkSourceTermData`, `TraceScaleNoExtraBulkContract` |
| no-extra-bulk reducer | no-extra-bulk source-term data can now be reconstructed from existing trace-scale no-missing-bulk data | `no_extra_bulk_source_term_data_of_trace_scale_no_missing_bulk`, `no_extra_bulk_contract_of_trace_scale_no_missing_bulk` |
| restricted-to-full | broad witness reduced to one threshold bridge package, and the normalized certificate/RH endpoints now consume that package directly | `RestrictedToFullThresholdBridgePackage`, `route_certificate_of_normalized_comparison_ledger_restricted_package`, `final_rh_of_normalized_comparison_ledger_restricted_package` |
| sign/defect | classification and source-backed ledger evidence can be built from one `LedgerSignDefectPackage`, and the normalized certificate/RH endpoints now consume that package directly | `LedgerSignDefectPackage`, `route_certificate_of_normalized_comparison_ledger_package_current_cutoff`, `final_rh_of_normalized_comparison_ledger_package_current_cutoff` |
| final sign | archimedean sign leg can come from source trace read-off | `source_archimedean_sign_bridge_of_source_trace_read_off` |
| normalized final endpoint | normalized route certificate and RH skeleton consume the ledger/sign-defect package, restricted-to-full threshold package, and final-exit package path directly | `route_certificate_of_normalized_comparison_ledger_restricted_package`, `normalizedFinalExitPackageFromTheorems`, `mathlib_rh_of_route_final_exit_package` |
| normalized endpoint | contract-backed endpoint exists | `final_rh_of_normalized_comparison_contract_replacement` |
| normalized source/fixed/trace front-end package | route package closed for the main normalized lane and the scalar trace lane; the normalized lane no longer projects triple-vanishing or lambda from the generic checklist front-end skeleton, and the scalar fixed-test now consumes the same explicit triple-vanishing theorem input instead of borrowing the original fixed front end | `SourceFixedTraceFrontEndPackage`, `normalizedFrontEndPackageFromTheorems`, `normalizedFrontEndPackageSourceTraceReadOffData_eq`, `normalizedScalarFrontEndPackageFromTheorems`, `normalizedScalarFrontEndPackageSourceTraceReadOffData_eq`, `normalizedFixedSTestTripleVanishingInputFromTheorems`, `normalizedTraceLambdaInputFromTheorems` |
| normalized no-argument outlet | Dev package consumes the normalized route certificate and final-exit package with no remaining endpoint arguments | `NormalizedNoArgumentRouteCertificatePackage`, `normalizedNoArgumentRouteCertificatePackageFromTheorems`, `mathlib_rh_of_normalized_no_argument_route_certificate_package`, `unconditional_rh_contract_skeleton` |

Interpretation:

```text
Old state:
  broad route inputs with several hidden mathematical subclaims

Current state:
  smaller named contracts plus an active actual-source scalar-identification
  theorem

Still missing:
  theorem evidence that the actual source objects satisfy the remaining
  source/front-end, ledger, restricted-to-full, and C.1 input contracts
```

## Evidence Scale

Use this scale for every remaining row.

| status | meaning | allowed action |
|---|---|---|
| Green | local theorem or clean import proves the exact row | consume it downstream |
| Yellow | exact target exists and definitions exist, but proof work remains | prove the target |
| Orange | exact target exists, but a shared definition or normal form is missing | define the missing object |
| Red | analytic theorem is missing from Lean | formalize, import, or prove it |
| Skeleton | temporary `sorry` exists in quarantined development code | route-design only |

Proof packages, comments, route ledgers, and wrappers do not make a row Green.
A row turns Green only when Lean has the exact theorem or a clean accepted
import for the exact theorem.

## Current Blockers

These are the current blockers for a no-argument `unconditional_rh`. Blocker 2
has been closed as an independent input, but it still depends on the
restricted-to-full evidence tracked in blocker 5.

Current distance:

```text
Route plumbing:
  Green through the normalized Dev no-argument outlet.

Certified theorem content:
  Red/Orange until 26 quarantined Dev inputs are supplied:

  A. CCM24 source model:
     5 inputs: semilocal symbols plus canonical, support, bounded, Sonin rows

  B. CCM25 source model:
     6 inputs: Weil symbols plus QW, Psi, QW_lambda, finite-prime, pole rows

  C. CC20/S2-B1 source seed and scalar rows:
     5 inputs: normalized seed, remainder rows, trace-package remainders,
     actual scalar identification, remaining-five-row constructor package

  D. source object / bridge / scalar / RH-exit rows:
     9 inputs: CC20 RH exit object, finite-prime exact data, CCM24 object,
     RH-exit object, RH-exit bridge equality, bridge/read-off rows,
     scalar remainder rows, scalar finite-part normal form, scalar common-test
     bridge rows

  E. fixed-test source input:
     1 input: triple vanishing
```

The practical distance is therefore not "one final theorem wrapper."  The
wrapper exists in Dev.  The missing work is to replace quarantined theorem
inputs with accepted-source imports or concrete Lean proofs that satisfy the
exact row records already wired into the route.

### 1. Source Package And Front Ends

Status: Green route/front-end package and normalized dependency cut,
Red/Orange theorem evidence.  The
formal `SourceFixedTraceFrontEndPackage` now binds the normalized
`SourceObjectPackage`, fixed front end, trace data, trace front end,
`SourceBackedFixedSTest`, and `SourceTraceReadOffData` to the same object
chain.  The normalized trace front end and final archimedean sign bridge now
project from this package.  The normalized lane also no longer borrows
`fixedFrontEndFromTheorems` or `traceFrontEndFromTheorems` from the generic
top-level checklist skeleton for triple-vanishing or lambda.  The remaining
work is theorem evidence for the source rows consumed by the package, not
another front-end wrapper.

Goal:

```lean
def sourceObjectPackageFromTheorems :
    Source.SourceObject.SourceObjectPackage := ...

def fixedFrontEndFromTheorems :
    ExpandedSourceFixedSTestFrontEnd sourceObjectPackageFromTheorems := ...

def traceFrontEndFromTheorems :
    ExpandedSourceTraceReadOffFrontEnd
      sourceObjectPackageFromTheorems fixedFrontEndFromTheorems := ...
```

Current blockers:

| subtarget | status | why it remains |
|---|---|---|
| normalized front-end dependency cut | Green skeleton cut | `normalizedFixedSTestTripleVanishingInputFromTheorems` and `normalizedTraceLambdaInputFromTheorems` are explicit normalized theorem inputs; repository search shows generic `fixedFrontEndFromTheorems` / `traceFrontEndFromTheorems` only in the top checklist skeleton block |
| CCM24 package and fixed-S window transport | Red/Orange | fixed support, Fourier support, convolution support, and window containment still need theorem/import evidence |
| CCM25 concrete common finite-prime seed | Green/Yellow | `SourceObjectConcreteCommonData` now ties the common source test to fixed-lambda arithmetic certificates, concrete finite-prime objects, global/restricted index data, restricted/global index-set equality, and finite-prime sum read-offs; `normalizedConcreteCommonTestFromTheorems` is now the definitional `concreteCommonSourceTest` built from the common test function projected out of `normalizedCommonFinitePrimeArithmeticSourceDataFromTheorems`; the common `(g,g)` source-test read-off and fixed-lambda certificate data project from the same unified object; broader all-test package and final support/window matching still remain |
| CCM25 arithmetic package | Yellow/Orange | package-level formula read-offs exist, and the concrete `(g,g)` common-test seed is theorem-backed; `normalizedConcreteArithmeticRowsFromTheorems` is now assembled from explicit global `QW/Psi` fields, the restricted `QW_lambda` formula row, finite-prime arithmetic certificate source-test data, and the pole-normalization theorem; the fixed-lambda certificate support/source-test equality is now definitional from `FixedLambdaArithmeticCertificateSourceTestData.toCertificate`; exact final support/window matching and downstream package consumption still matter |
| CC20 trace package | Green for the actual scalar row; Red/Orange for remaining source theorem inputs | `normalizedSeedQWLambdaScalarIdentificationOfTheoremBasePackage` closes the actual source-seed support-square/`QW_lambda` scalar identity, and the normalized trace lane consumes it; the remaining CC20 trace-package work is theorem evidence for the normalized seed, trace model, scalar remainder rows, common-test/read-off rows, and C.1/RH exit fields needed by the source package |
| CC20 RH exit package | Red | Proposition C.1 and source-RH bridge must be theorem-backed |
| cross-object common-test bridges | Yellow/Orange | named records exist, but final path must not consume loose source-package `Prop` fields |

Do not spend more time reshaping interfaces unless the edit removes, weakens,
or proves one of these inputs.

Latest dependency cut:

```lean
def normalizedConcreteCommonFromTheorems :
    Source.SourceObjectConcreteCommonData normalizedBaseFromTheorems :=
  Source.SourceObjectConcreteCommonData.ofCommonPairSourceTest
    normalizedConcreteCommonTestFromTheorems
    normalizedConcreteArithmeticRowsFromTheorems
    normalizedCommonPairSourceTestFromTheorems

def normalizedCommonFromTheorems :
    Source.SourceObjectCommonData normalizedBaseFromTheorems :=
  normalizedConcreteCommonFromTheorems.toSourceObjectCommonData
```

This removes the normalized lane's direct `SourceObjectCommonData` black box.
The fixed-lambda arithmetic-certificate source-test read-off is also no longer
an independent input:

```lean
theorem normalizedFixedLambdaArithmeticCertificateSourceTestFromTheorems
    (f g : TestFunction) (lambda : ℝ) (hlambda : 1 < lambda) :
    ((normalizedFixedLambdaArithmeticCertificatesFromTheorems
          f g lambda hlambda).support.sourceTest =
      normalizedFinitePrimeSourceTestFromTheorems f g) :=
  rfl
```

The common-pair finite-prime selector specialization is now part of the
selector data rather than a separate theorem input:

```lean
structure FinitePrimeSourceTestSelectorData
    (W : WeilFormSymbols)
    (common :
      Source.CCM25Concrete.CommonSourceTest.ConcreteCommonSourceTest W) where
  sourceTest :
    ∀ f g : TestFunction,
      Source.CCM25Concrete.PrimePowerTest.SourceTestEvaluationInterface W f g
  commonPairSourceTest :
    sourceTest common.sourceTest common.sourceTest =
      common.toSourceTestEvaluationInterface
```

The common source test, selector data, and fixed-lambda certificate data now
come from a single finite-prime arithmetic source object:

```lean
structure CommonFinitePrimeArithmeticSourceData
    (W : WeilFormSymbols) where
  commonTestFunction : TestFunction
  finitePrimeData :
    FinitePrimeArithmeticSourceData W
      (Source.CCM25Concrete.CommonSourceTest.concreteCommonSourceTest
        W commonTestFunction)

structure FinitePrimeArithmeticSourceData
    (W : WeilFormSymbols)
    (common :
      Source.CCM25Concrete.CommonSourceTest.ConcreteCommonSourceTest W) where
  selector : FinitePrimeSourceTestSelectorData W common
  certificateData :
    ∀ f g : TestFunction, ∀ lambda : ℝ,
      FixedLambdaArithmeticCertificateSourceTestData
        W f g lambda (selector.sourceTest f g)
```

The remaining proof inputs are narrower:

| input | meaning |
|---|---|
| `normalizedGlobalQWPsiRowsFromTheorems` | the global `QW` definition row and global `Psi` sign row, carried by one source row object |
| `normalizedRestrictedQWLambdaPoleRowsFromTheorems` | the restricted `QW_lambda` formula row and pole-normalization row, carried by one source row object |
| `normalizedCommonFinitePrimeArithmeticSourceDataFromTheorems` | the fixed CCM25 common source test function, source-test selector, common `(g,g)` read-off, fixed-lambda support, exact index data, lambda-cut visibility, arithmetic atoms, and proof that those atoms use the selected source test |
| actual-source scalar identification | closed at active Source theorem level by `normalizedSeedQWLambdaScalarIdentificationOfTheoremBasePackage`; the normalized trace fixed-tuple row consumes it through `normalizedActualScalarIdentificationFromTheoremBasePackage` |

### 2. Scalar Full-Trace Pole Balance

Status: Green relative to restricted-to-full evidence.

The finite-prime half has been reduced to a theorem-backed package equality.
The remaining target is the archimedean/pole identity:

```text
archimedeanTerm(F_g) + polePairing(g)
  =
poleFunctional(F_g) - archimedeanTerm(F_g)
```

Lean target:

```lean
TraceFrontEndData.NormalizedScalarFullTraceArchimedeanPoleBalance
```

Current Lean reducers:

```lean
theorem normalizedScalarFullTraceArchimedeanPoleBalanceOfQWLambdaRestriction
theorem normalizedScalarFullTraceArchimedeanPoleBalanceOfRestrictedToFullContract
```

Why it matters:

```text
pole balance
        |
        v
NormalizedScalarFullTraceArchimedeanBalance
        |
        v
scalar full trace read-off bridge
```

The old broad `NormalizedScalarFullTraceArchimedeanBalance` input is no longer
the main target. The pole-balance contract now follows from the same
restricted-to-full evidence that proves the `QW_lambda -> QW` bridge: use the
archimedean contribution equality, rewrite the restricted finite-prime sum to
the global finite-prime sum, then cancel the common finite-prime term.

Boundary:

```text
This closes the scalar pole balance as a separate route input.
It does not prove restricted-to-full evidence itself.
```

### 3. No-Extra-Bulk And No-Missing-Bulk

Status: Green source row package; Red/Orange theorem evidence for the row
inputs.

Current Lean target:

```lean
TraceFrontEndData.TraceScaleNoExtraBulkSourceTermData
TraceFrontEndData.TraceScaleNoExtraBulkContract
```

The source-term data now exposes the exact S2-B1 checks before it is consumed
by the route contract:

| field | role |
|---|---|
| `positiveTraceDecomposesIntoQWLambdaRankPoleCdef` | positive trace decomposes only into `QW_lambda`, rank, pole, and endpoint-strip `Cdef` channels |
| `noBulkScaleTermOutsideLedger` | blocks an extra `BulkScaleTerm_(S,I,lambda,g)` outside those channels |
| `noHiddenFinitePartSubtraction` | blocks a hidden finite-part subtraction or scalar renormalization |

`TraceScaleNoExtraBulkContract` then exposes:

| field | role |
|---|---|
| `rankPoleCdefOwnEveryRemainder` | every sign/defect classification owns the rank, pole, and `Cdef` channels |
| `noExtraBulkScaleTerm` | the named no-extra-bulk proposition |
| `noExtraBulkScaleTermHolds` | proof of the no-extra-bulk proposition |

Why it matters:

```text
sign/defect classification
        |
        v
rank/pole/Cdef ownership
        |
        v
no missing positive bulk
        |
        v
trace-scale route front end
```

The current route no longer hides this behind `True`.  The
`S2B1FixedTupleRemainingRowsPackage` now packages the five rows left after the
support-square scalar leg: rank/zero-mode, no-strip rank/pole, endpoint-strip
`Cdef`, no-extra-bulk, and no-hidden finite-part subtraction.  The next step is
to supply theorem-backed row inputs for that package and the trace-scale
no-missing-bulk data, not to create another wrapper around the same rows.

The no-hidden finite-part row now has the sharpest current target shape:

```text
S2B1FinitePartSourceScalarData
        |
        v
S2B1FinitePartSourceNormalFormData
        |
        +-- actualFinitePart = normalizedFinitePart
        |
        +-- subtractedFinitePartTerm = 0
        |
        v
S2B1NoHiddenFinitePartSubtractionConstructorRows
```

This split matters because finite-part normal form must not be identified with
`supportSquareTrace`, `sourceNoDefectTrace`, `traceAmplitude ^ 2`, or
`QW_lambda` by name.  Prove the finite-part row from its own source scalar
normal form, then let the existing constructors carry it into the five-row
S2-B1 package.

Once a `TraceScaleNoMissingBulkData` value is available, Lean derives
`TraceScaleNoExtraBulkSourceTermData` and `TraceScaleNoExtraBulkContract`
through `no_extra_bulk_source_term_data_of_trace_scale_no_missing_bulk` and
`no_extra_bulk_contract_of_trace_scale_no_missing_bulk`.

Latest dependency cut:

```lean
structure NormalizedScalarTraceScaleInputData where
  fullTraceReadOffBridge : FullTraceReadOffBridgeContract ...
  traceScaleNoMissingBulk : TraceScaleNoMissingBulkData ...
  traceAmplitudeSquareScalar :
    TraceFrontEndData.NormalizedTraceAmplitudeSquareScalarContract ...
```

The normalized scalar lane now consumes this single data-bearing input package
for the scalar full-trace bridge, trace-scale no-missing-bulk data, and trace
amplitude square scalar contract. This removes three separate skeleton
placeholders without pretending to prove the S2-B1 analytic row inputs. The
remaining mathematical target is theorem-backed evidence for those row inputs.

The amplitude-square scalar leg now has a route-evidence proof package:

```text
docs/proofs/normalized-trace-amplitude-square-scalar-proof-package.md
```

It proves the intended chain:

```text
traceAmplitude(a)^2
        =
supportSquareTrace(a)
        =
NoDefectSourceTrace(S,I,lambda,g)
        =
QW_lambda(g,g)
        =
NormalizedRestrictedScalarNormalForm(...)
```

Lean has the endpoint contract and the local reducers:

| item | evidence |
|---|---|
| target contract | `ConnesWeilRH/Route/TraceFrontEnd.lean:2324`, `NormalizedTraceAmplitudeSquareScalarContract` |
| support-square-to-amplitude reducer | `ConnesWeilRH/Route/TraceFrontEnd.lean:2396`, `normalizedTraceAmplitudeSquareScalarContractOfSupportSquareScalarNormalForm` |
| open skeleton input | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:882`, `normalizedSupportSquareScalarNormalFormInputFromTheorems` |
| amplitude skeleton reducer | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:891`, `normalizedTraceAmplitudeSquareScalarInputFromTheorems` |
| support-square equals trace-amplitude square | `ConnesWeilRH/Route/TraceFrontEnd.lean:2200`, `normalized_cc20_support_square_normal_form_eq_trace_amplitude_square` |
| `QW_lambda` equals the restricted scalar normal form | `ConnesWeilRH/Route/TraceFrontEnd.lean:2453`, `normalized_qw_lambda_reduces_to_normal_form` |

This no longer remains as a loose scalar input. Lean has two different paths
that must not be confused:

```text
matched seed path
        |
        v
traceAmplitude := sqrt(CCM25 restricted evaluator)
        |
        v
supportSquareTrace = QW_lambda
```

```text
actual source route path
        |
        v
source CC20 trace object
        |
        v
supportSquareTrace = QW_lambda    <-- closed by theorem-base scalar identity
```

The matched seed path is implemented by
`normalizedSeedMatchedSupportSquareQWLambdaReadOff`. The actual source route
path now has the active theorem
`normalizedSeedQWLambdaScalarIdentificationOfTheoremBasePackage`, which proves
`NormalizedSeedQWLambdaScalarIdentification` from the theorem-base
support-square row and the CC20 support-square read-off.

Boundary:

```text
Matched seed fixed tuple:
  support-square row is Green under evaluator nonnegativity.

Actual source route tuple:
  support-square row is Green in the normalized trace lane.
  The early no-argument Dev skeleton input remains quarantined.
```

Do not add another wrapper around the same support-square equality. The closed
path is:

| target | current state |
|---|---|
| `NormalizedSeedQWLambdaScalarIdentification` | closed by `normalizedSeedQWLambdaScalarIdentificationOfTheoremBasePackage` |
| normalized trace fixed-tuple support-square row | consumes `normalizedActualScalarIdentificationFromTheoremBasePackage` |
| no-argument Dev skeleton input | still quarantined as `normalizedCoreS2B1ActualScalarIdentificationFromTheorems` |

Boundary:

```text
TraceScaleNoMissingBulkData
        |
        v
TraceScaleNoExtraBulkSourceTermData
        |
        v
TraceScaleNoExtraBulkContract
```

This closes the source-term wrapper as an independent skeleton input. It does
not prove the analytic `noExtraBulkScaleTerm`; that proof still lives inside
the trace-scale no-missing-bulk data.

### 4. Ledger Clearing And Sign/Defect Classification

Status: Green route package relative to source ledger-row evidence; Red/Orange
for proving those ledger rows from accepted source or concrete Lean theorems.

Current route package:

```lean
structure LedgerSignDefectPackage
    (inputs : RouteInputs) (g : SourceBackedFixedSTest inputs)
    (lambda : ℝ) where
  ledgers : RouteLedgers
  ledgersCleared : LedgersCleared ledgers
  sourceBackedLedgers : SourceBackedLedgers inputs g ledgers
  sourceBackedLedgersLambda : sourceBackedLedgers.lambda = lambda
  signDefectClassification :
    SourceSignDefectClassification inputs g lambda ledgers
```

Current reducer:

```lean
ledger_sign_defect_package_of_ledgers_cleared
ledger_sign_defect_package_of_parts
source_sign_defect_classification_of_ledgers_cleared
source_backed_ledgers_of_ledgers_cleared
```

Why it matters:

```text
rankKilled + poleKilled + cdefExhausts + 1 < lambda
        |
        v
SourceSignDefectClassification
        |
        v
lower-bound evidence + no-extra-bulk + full positivity
```

The sign/defect bridge now has a formal route-level package instead of a
Dev-only skeleton container.  The normalized skeleton consumes
`LedgerSignDefectPackage`, then projects `RouteLedgers`, `rankKilled`,
`poleKilled`, `cdefExhausts`, `LedgersCleared`,
`SourceSignDefectClassification`, and `SourceBackedLedgers` from that package.
The normalized certificate and RH endpoints also consume the same package
directly, so final-route construction no longer accepts loose ledger and
sign/defect pieces when the package is available.

Evidence:

| item | evidence | status |
|---|---|---|
| formal package | `ConnesWeilRH/Route/Ledger.lean:84`, `LedgerSignDefectPackage` | Green route package |
| constructor from cleared ledgers | `ConnesWeilRH/Route/Ledger.lean:147`, `ledger_sign_defect_package_of_ledgers_cleared` | Green route reducer |
| constructor from rank/pole/`Cdef` rows | `ConnesWeilRH/Route/Ledger.lean:160`, `ledger_sign_defect_package_of_parts` | Green route reducer |
| normalized route skeleton consumes formal package | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:3316`, `normalizedRouteLedgerClearingInputDataFromTheorems` | Green wiring, skeleton source rows remain |
| source-backed ledgers and sign/defect now project from the same package | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:3348`, `normalizedSourceBackedLedgersFromTheorems`; `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:3358`, `normalizedSignDefectClassificationFromTheorems` | Green wiring |
| normalized certificate endpoint consumes package | `route_certificate_of_normalized_comparison_ledger_package_current_cutoff` | Green route endpoint wiring |
| normalized RH endpoint consumes package | `final_rh_of_normalized_comparison_ledger_package_current_cutoff` | Green conditional endpoint wiring |

Boundary:

```text
Closed in Lean:
  rankKilled + poleKilled + cdefExhausts + 1 < lambda
        |
        v
  LedgerSignDefectPackage
        |
        v
  LedgersCleared + SourceBackedLedgers + SourceSignDefectClassification
        |
        v
  normalized RouteCertificate / RH endpoint package consumption

Still open:
  accepted-source / concrete Lean proof of the three ledger rows themselves
```

### 5. Restricted-To-Full Threshold Evidence

Status: Green route package relative to asymptotic threshold rows, current
cutoff evidence, and sign/defect lower-bound evidence; Red/Orange for proving
the asymptotic source rows from accepted source or concrete Lean theorems.

Current route package:

```lean
structure RestrictedToFullThresholdBridgePackage
    (inputs : RouteInputs) (g : SourceBackedFixedSTest inputs)
    (lambda : ℝ) (F_g : TestFunction) (L : RouteLedgers)
    (pkg : ConcreteCCM25ArithmeticPackage ...) where
  currentCutoff :
    RestrictedToFullCurrentCutoffBinding inputs g lambda F_g L pkg
  bridgeContract :
    RestrictedToFullQWBridgeContract inputs g lambda F_g L pkg
  bridgeContractMatchesCurrentCutoff :
    bridgeContract =
      restricted_to_full_bridge_contract_of_current_cutoff_binding
        currentCutoff
```

Current reducer:

```lean
source_common_test_tuple_contract_of_package
restricted_to_full_current_cutoff_binding_of_sign_defect
restricted_to_full_threshold_bridge_package_of_sign_defect
restricted_to_full_bridge_contract_of_current_cutoff_binding
restricted_to_full_bridge_contract_of_threshold_package
route_certificate_of_normalized_comparison_current_cutoff_binding
final_rh_of_normalized_comparison_current_cutoff_binding
route_certificate_of_normalized_comparison_ledger_restricted_package
final_rh_of_normalized_comparison_ledger_restricted_package
```

Why it matters:

```text
common tuple
        |
        v
same g, same F_g, same lambda, same arithmetic package
        |
        v
finite-prime support stabilization + scalar restriction witness
        |
        v
QW_lambda(g,g) = QW(g,g)
```

The common tuple is no longer an independent skeleton dependency in the generic
or normalized development lanes. It is now built from
`window_lambda_compatibility_of_source_backed` plus
`expanded_source_package_convolution_square_read_off` via
`source_common_test_tuple_contract_of_package`. The remaining inputs are the
large-lambda threshold package, current-above-threshold proof, scalar
restriction witness supplied by the threshold package.

The lower-bound evidence is also no longer an independent restricted-to-full
input. `restricted_to_full_current_cutoff_binding_of_sign_defect` derives it
from `SourceSignDefectClassification` through Row 7 endpoint-strip `Cdef`
domination before constructing the current-cutoff binding.

The current-cutoff binding records that the current threshold data and scalar
witness are tied to the same typed common tuple. Because the common tuple is a
`Prop`, equality of proof objects is proof-irrelevant; the real drift guard is
the shared `g`, `F_g`, `lambda`, and `pkg` in the binding type.

The normalized certificate/RH skeleton now consumes a formal
`RestrictedToFullThresholdBridgePackage`. The current-cutoff binding and
`RestrictedToFullQWBridgeContract` project from the same package, and the
package stores an equality proving that the bridge contract is exactly the one
derived from the current cutoff.

Evidence:

| item | evidence | status |
|---|---|---|
| formal threshold bridge package | `ConnesWeilRH/Route/Bridge.lean:2791`, `RestrictedToFullThresholdBridgePackage` | Green route package |
| constructor from current cutoff | `ConnesWeilRH/Route/Bridge.lean:2806`, `restricted_to_full_threshold_bridge_package_of_current_cutoff` | Green route reducer |
| constructor from sign/defect lower-bound evidence | `ConnesWeilRH/Route/Bridge.lean:2835`, `restricted_to_full_threshold_bridge_package_of_sign_defect` | Green route reducer |
| normalized route skeleton consumes formal package | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:3680`, `normalizedRestrictedToFullThresholdBridgePackageFromTheorems` | Green wiring, upstream rows remain |
| normalized `QW_lambda -> QW` bridge projects from the same package | `ConnesWeilRH/Dev/UnconditionalSkeleton.lean:3727`, `normalizedRestrictedToFullQWFromTheorems` | Green wiring |
| normalized certificate endpoint consumes formal package | `route_certificate_of_normalized_comparison_ledger_restricted_package` | Green route endpoint wiring |
| normalized RH endpoint consumes formal package | `final_rh_of_normalized_comparison_ledger_restricted_package` | Green conditional endpoint wiring |

Boundary:

```text
Closed in Lean:
  large-lambda threshold + current cutoff + common tuple + sign/defect
        |
        v
  RestrictedToFullThresholdBridgePackage
        |
        v
  RestrictedToFullCurrentCutoffBinding
        |
        v
  RestrictedToFullQWBridgeContract
        |
        v
  QW_lambda(g,g) = QW(g,g)

Still open:
  accepted-source / concrete Lean proof of the asymptotic rows feeding the
  threshold package, especially the source archimedean balance row
```

Do not replace this with a limit, spectral convergence, or determinant
convergence claim unless that claim is formalized and tied to the fixed
finite-prime support theorem.

### 6. Final Certificate And RH Exit

Status: Green route final-exit package relative to a `RouteCertificate`, and
Green no-argument Dev package consumption for the normalized lane. Red remains
for replacing the quarantined Dev theorem inputs with theorem-backed
source/front-end/S2-B1 rows.

Goal:

```lean
def routeCertificateFromTheorems :
    RouteCertificate
      (RouteInputs.ofExpandedSourcePackage sourceObjectPackageFromTheorems)
    := ...

theorem unconditional_rh : _root_.RiemannHypothesis := by
  exact final_connes_weil_rh routeCertificateFromTheorems
```

This is the final assembly step. It may happen only after blockers 1 through 5
have theorem-grade evidence.

Current route package:

```lean
structure RouteFinalExitPackage (inputs : RouteInputs) where
  certificate : RouteCertificate inputs
  exitInput : RouteBackedCC20ExitInputData inputs ...
  sourceRH : inputs.cc20.rhDefinitionBridge.SourceRH
  sourceRHMatchesC1 :
    sourceRH =
      inputs.cc20.cc20RHExitObjectPackage.propositionC1SourceCriterion
        exitInput.input exitInput.propositionC1InputData
  mathlibRH : _root_.RiemannHypothesis
  mathlibRHMatchesSourceBridge :
    mathlibRH =
      Source.RHDefinitionBridge.source_rh_to_mathlib_rh
        inputs.cc20.rhDefinitionBridge sourceRH
```

Evidence:

| item | evidence | status |
|---|---|---|
| formal final-exit package | `ConnesWeilRH/Route/RouteTheorem.lean:1298`, `RouteFinalExitPackage` | Green route package |
| constructor from route certificate | `ConnesWeilRH/Route/RouteTheorem.lean:1315`, `route_final_exit_package_of_certificate` | Green route reducer |
| source RH projection | `ConnesWeilRH/Route/RouteTheorem.lean:1349`, `cc20_source_rh_of_route_final_exit_package` | Green route reducer |
| Mathlib RH projection | `ConnesWeilRH/Route/RouteTheorem.lean:1354`, `mathlib_rh_of_route_final_exit_package` | Green route reducer |
| final theorem consumes package | `ConnesWeilRH/Route/RouteTheorem.lean:2577`, `final_connes_weil_rh` | Green conditional endpoint |
| normalized Dev skeleton consumes final-exit package | `normalizedFinalExitPackageFromTheorems`; `unconditional_rh_contract_skeleton` now projects through `mathlib_rh_of_route_final_exit_package` | Green Dev endpoint wiring; source inputs remain |
| normalized no-argument Dev package | `NormalizedNoArgumentRouteCertificatePackage`, `normalizedNoArgumentRouteCertificatePackageFromTheorems`, `mathlib_rh_of_normalized_no_argument_route_certificate_package` | Green Dev package consumption; quarantined theorem inputs remain |

Additional final-exit obligations:

| target | status |
|---|---|
| CC20 finite-vanishing criterion / Proposition C.1 | Green as consumed by `RouteFinalExitPackage`, conditional on source package field |
| source Mellin vanishing from route triple vanishing | Green inside `RouteBackedCC20ExitInputData`, conditional on `RouteCertificate` |
| source RH to Mathlib `_root_.RiemannHypothesis` bridge | Green through `RHDefinitionBridge.source_rh_to_mathlib_rh` |
| final axiom audit for `final_connes_weil_rh` | Green; no `sorryAx` |
| final axiom audit for no-argument `unconditional_rh` | Red until no-argument route certificate exists |

Boundary:

```text
Closed in Lean:
  RouteCertificate
        |
        v
  RouteBackedCC20ExitInputData
        |
        v
  CC20 Proposition C.1 source criterion
        |
        v
  SourceRH
        |
        v
  Mathlib _root_.RiemannHypothesis

Still open:
  no-argument construction of RouteCertificate from theorem-backed source
  package/front-end/S2-B1 evidence
```

## Fastest Execution Path

This is the shortest path from the current code.

Main instruction: stop adding route wrappers unless they delete a quarantined
input. The endpoint packaging is already closed. Attack theorem evidence.

1. Keep the actual-source support-square scalar target closed through
   `normalizedSeedQWLambdaScalarIdentificationOfTheoremBasePackage`. The proof
   path is `supportSquareTrace = traceAmplitude^2` plus
   `supportSquareTrace = QW_lambda`, then symmetry/transitivity.

2. Treat scalar full-trace pole balance as a dependent reducer. It is no
   longer an independent target once restricted-to-full evidence is available.

3. Keep the actual-source S2-B1 fixed-tuple theorem on the fixed-tuple route,
   not the stronger all-test compatibility package. The Lean target shape is
   `S2B1TraceScaleFixedTupleAnalyticExclusionPackage`: one fixed cutoff
   `lambda`, one CC20 archimedean trace test, and one CCM25 Weil test. The
   support-square leg now comes from the proved scalar identity; the remaining
   five rows come from `S2B1FixedTupleRemainingRowsPackage`. In that package,
   attack no-hidden finite-part through `S2B1FinitePartSourceNormalFormData`,
   not through a free equality against support-square or `QW_lambda`. Only
   promote to the older all-`lambda`/all-test
   `S2B1TraceScaleAnalyticExclusionPackage` if a later consumer really needs
   that stronger statement.

4. Use those S2-B1 rows to keep `NormalizedScalarTraceScaleInputData` theorem
   backed. The no-extra-bulk source-term wrapper already follows from the
   data-bearing no-bulk witness path; the remaining risk is no-argument
   certificate extraction, not another fixed-tuple row wrapper.

5. Do not promote the pointwise scalar route-test certificates into
   `normalizedScalarRemaindersFromTheorems`. The scalar remainder package needs
   global scalar-package data, including a Hilbert-Schmidt gate for every test
   in the scalar seed. Current route certificates only justify the current
   route test.

6. Supply theorem-backed rank, pole, and endpoint-strip `Cdef` ledger rows.
   `LedgerSignDefectPackage` now turns those rows and `1 < lambda` into
   `LedgersCleared`, `SourceBackedLedgers`, and
   `SourceSignDefectClassification`.

7. Supply theorem-backed asymptotic restricted-to-full rows. The formal
   `RestrictedToFullThresholdBridgePackage` now ties the large-lambda
   threshold, current cutoff, common tuple, sign/defect lower-bound evidence,
   scalar restriction witness, and final `QW_lambda -> QW` bridge to the same
   fixed test, cutoff, and arithmetic package.

8. Build source package, fixed front end, trace front end, current-cutoff
   binding, route front end, and route certificate internally. The normalized
   source/fixed/trace front-end part now closes through
   `SourceFixedTraceFrontEndPackage`; the final C.1/RH-definition exit now
   follows through `RouteFinalExitPackage`.

9. Add `unconditional_rh` only after the placeholder scan, build, and axiom
   audit pass.

Concrete proof attack:

```text
Step A
  Finish the three source-model body lane:
    - CCM24 fixed-S window/support transport
    - CCM25 exact Weil-form and finite-prime rows
    - CC20 normalized seed, remainder rows, and scalar identification

Step B
  Replace normalized source-object theorem-base inputs with source-backed
  records:
    - finite-prime exact/common-test arithmetic data
    - CC20 RH-exit object
    - bridge/read-off rows

Step C
  Prove the five remaining S2-B1 fixed-tuple rows at the exact current tuple:
    - rank/zero-mode
    - no-strip rank/pole
    - endpoint-strip Cdef
    - no-extra-bulk
    - no-hidden finite-part subtraction, via:
        actualFinitePart = normalizedFinitePart
        subtractedFinitePartTerm = 0

Step D
  Prove the ledger rows from Step B and the sign/defect package:
    - rankKilled
    - poleKilled
    - cdefExhausts

Step E
  Prove restricted-to-full asymptotic rows:
    - threshold support stabilization
    - prime-power atom stabilization
    - scalar restriction witness
    - source archimedean balance

Step F
  Move the Dev no-argument outlet into the certified route only after
  `rg` finds no `sorry`/`admit`/new `axiom` in the imported theorem path and
  `#print axioms unconditional_rh` has no `sorryAx`.
```

Concrete attack order:

| order | target | current color | done means |
|---|---|---|---|
| 1 | CC20 source-model body | Green body, one seed input remains | `normalizedCoreCC20TraceModelFromTheorems` stays definitionally generated from `normalizedCoreS2B1NormalizedSeedFromTheorems`; no separate CC20 trace-model row holes return |
| 2 | CCM24 source-model body | Red/Orange theorem evidence | replace the five CCM24 skeleton inputs with source-backed canonical, support, bounded, and Sonin transport theorems |
| 3 | CCM25 source-model body | Red/Orange theorem evidence | replace the six CCM25 skeleton inputs with exact QW/Psi/QW_lambda, finite-prime, and pole-normalization theorem evidence |
| 4 | normalized seed, remainder rows, and actual scalar identification | Yellow/Orange | prove the normalized seed source object, trace-package remainders, and `NormalizedSeedQWLambdaScalarIdentification` without downstream circular projection |
| 5 | remaining S2-B1 fixed-tuple rows | Green source row package, skeleton theorem inputs remain | `S2B1FixedTupleRemainingRowsPackage` carries rank/zero-mode, no-strip rank/pole, endpoint-strip `Cdef`, no-extra-bulk, and no-hidden finite-part rows; no-hidden finite-part reduces to `finitePartNormalizationFixedStatement` and `noSubtractedFinitePartTermStatement` |
| 6 | normalized source object / bridge / scalar rows | Green route package for main and scalar lanes, Red/Orange theorem evidence | source package, common-test bridges, trace read-off equalities, scalar remainder rows, scalar finite-part normal form, and scalar common-test bridge rows are theorem-backed |
| 7 | ledger clearing and sign/defect package | Green route package, Red/Orange source rows | `LedgerSignDefectPackage` is built from theorem-backed rank, pole, and endpoint-strip `Cdef` rows |
| 8 | restricted-to-full threshold package | Green route package, Red/Orange asymptotic rows | `RestrictedToFullThresholdBridgePackage` is built from theorem-backed asymptotic rows and sign/defect evidence |
| 9 | CC20 C.1 exit and RH definition bridge | Green route package and no-argument Dev package, Red theorem-input discharge | `RouteFinalExitPackage` projects C.1 source RH through the RH-definition bridge to Mathlib RH; `NormalizedNoArgumentRouteCertificatePackage` is the single normalized no-argument Dev outlet |

## Verification Commands

Roadmap edit:

```text
git diff --check
rg -n "Current Blockers|Fastest Execution Path|unconditional_rh|RouteCertificate|TraceScaleNoExtraBulkContract|finitePartNormalizationFixedStatement|noSubtractedFinitePartTermStatement" external-opinions/003-unconditional-rh-completion-plan.md
```

Current placeholder gate:

```text
rg -n "\bsorry\b|\badmit\b|^\s*(axiom|constant|opaque|unsafe)\b" ConnesWeilRH -g "*.lean"
```

Lean development:

```text
lake build ConnesWeilRH.Route.TraceFrontEnd
lake build ConnesWeilRH.Route.Bridge
lake build ConnesWeilRH.Route.SignDefect
lake build ConnesWeilRH.Route.RouteTheorem
lake build ConnesWeilRH
```

Final certification:

```text
rg -n "\bsorry\b|\badmit\b|^\s*(axiom|constant|opaque|unsafe)\b" ConnesWeilRH -g "*.lean"
lake build ConnesWeilRH
lake env lean /tmp/connes_axiom_audit.lean
```

The audit file should contain at least:

```lean
import ConnesWeilRH
#check unconditional_rh
#print axioms unconditional_rh
```

## Status Language

Allowed before blocker 6 is Green:

```text
source-conditional route evidence
conditional Lean route composition
theorem skeleton
source-interface target
not yet unconditional RH
```

Forbidden before blocker 6 is Green:

```text
unconditional proof of RH
proved RH
completed Lean proof of RH
journal proof
Clay proof
accepted-source proof
```
