# Route map

Status: authoritative route index, compressed 2026-09-23. RH is not claimed.

This directory contains only records that can still change a future proof
decision: binding route rulings, active producer contracts, live analytic
obligations, reusable no-go results, and interfaces referenced by source or
the root README. Per-round ledgers and superseded plans belong in Git history
and `docs/proofs/`.

## Mandatory preflight

Before a new mathematical campaign, read in order:

1. [001](001_first_cut_window_architecture.md) for the ROOT-local base and its
   strict boundary;
2. [002](002_one_shot_rh_route_verdict.md) for the paper-scale audit and dead
   universal shortcut;
3. [003](003_b1_b5_minimal_exit_route_selection.md) for the binding healthy
   `CompactLog` B5 route;
4. [004](004_endpoint_literature_interface_audit.md) for the endpoint/C3
   interface and evidence levels.

Read [006](006_new_math_creation_workflow.md) before inventing a new analytic
mechanism. Read the active-lane record named below before editing its Lean
consumer. The project-wide freeze and core-progress gate are in
`RH_MAINLINE_FREEZE.md`.

## Canonical dependency graph

```text
hypothetical off-line zero
  -> healthy selected CompactLog detector g with qw(g) < 0       [formal]
  -> prove qw(g) >= 0 for that same selected owner               [OPEN]
  -> contradiction / SourceRH                                   [formal]
  -> Mathlib RiemannHypothesis                                  [formal]
```

The second line is the RH core. A new exit theorem, coordinate change,
conditional certificate, or generic tail lemma is not progress unless it
proves a named premise of that line or produces a checked no-go.

The normalized coverage root remains an RH-equivalent audit socket. Its
additive-convolution owner is not the producer owner. ROOT-window positivity
is a shared local base and reaches the selected detector only through a proved
matching support theorem or a genuine semi-local extension.

## Current producer state

The selected orbit detector, its strict spectral negativity, its
support-derived finite visible-prime owner, and the contradiction consumer are
formal. Detector-specific semi-local nonnegativity is open.

Two equivalent attack lanes remain admissible:

### A. Same-owner signed physical-kernel / C3' budget

- [047](047_detector_sign_energy_and_discrepancy_audit.md): exact sign
  bookkeeping and screened false shortcuts.
- [075](075_c3p_carrier_transport_after_1800.md): phase/readback reduction.
- [079](079_physical_node_certificate_route_socket.md): finite physical-node
  certificate socket.
- [080](080_c3p_signed_certificate_owner.md): current same-owner signed
  certificate and its exact missing field.
- [081](081_orbit_physical_kernel_coboundary_certificate.md): aggregate
  coboundary reduction.
- [082](082_direct_semilocal_gate_assault.md): shortest formal consumer to
  `SourceRH`.
- [104](104_route_a_four_round_campaign.md): active four-round execution plan;
  Round 1 exact selected-owner finite-profile readback and Round 2's
  derivative-controlled selector / exact aggregate-residual reduction are
  formal; the current selector has a scoped no-go for supplying the final
  signed margin.

This lane must prove an unconditional signed inequality on the actual selected
detector and its actual finite visible-prime set. Channelwise absolute
majorants, fixed-prime countermodels, and hypotheses equivalent to the desired
sign do not close it.

### B. Phase-balanced two-span spectral contradiction

Current execution priority is the same-span four-point subcampaign in [103](103_four_point_same_span_three_cut_campaign.md), because its parabola, prefix, and tail infrastructure are formal and its remaining gate obligation is an explicit aggregate determinant inequality. This is a priority choice, not a new binding route ruling.

- [090](090_finite_index_pinning_and_frozen_prime_domain.md): finite owner and
  pinned orbit.
- [091](091_two_span_sign_balancing_closed.md): semi-local nonnegative span.
- [092](092_mainline_exit_wiring_closed.md): formal contradiction consumer.
- [094](094_two_point_differential_annihilation_and_spectral_decomposition.md):
  current spectral-prefix transport.
- [103](103_four_point_same_span_three_cut_campaign.md): three-cut work order
  for the *same* four-point span, including the gate-owner mismatch.

The current exact obligation is a strict negative full spectral value and a
semi-local nonnegative gate for one and the same span. The finite-prefix
margin and an explicit weighted high-shell bound are formal for the four-point
annihilator span. The strict tail-to-`lambda^2` budget is open because the
gate-selected `lambda` may depend on the convolution count. The proved gate
applies to a different narrow-root span; the four-point span's own gate is the
exact parabola `D - lambda*B' + lambda^2*C` with a complete trichotomy
(proof record 1917), and the sign probe of record 1918 selects its vertex
branch on a committed-class family (`D > 0` and the cross-determinant
`D*C - (B'/2)^2 < 0` in the certified engine pair), so its remaining
obligation is that scored determinant inequality together with the positive
cross sum, with the strict witness formal at `lambda = B'/(2*C)` (records
1917/1918). Record 1919 rewrites that determinant exactly: the whole gate is a
spectral pairing with the explicit kernel `K = sigma(2*pi*xi) +
2*sum (Lambda(n)/sqrt n) cos(2*pi*xi*log n)`, so `D, B01, C` are moments of the
single signed measure `mu = K*W*dxi` (`W = |ghat|^2 >= 0`), and
`det = A^2 * Var_nu(P)` with the exact sign-split identity
`Var_nu(P) = (1+f)*Var_+ - f*Var_- - f*(1+f)*Delta^2`; the obligation is the
variance-gap (five-moment bracket) inequality, verified to floating-point
precision on four cases with the negative-spread term essential (`f*Delta^2/Var_+`
in `0.19 .. 0.94`). Record 1920 (scouting) locates the negativity in the
visible prime sum: `>= 99.7%` of the negative mass is prime-created inside the
`sigma`-positive zone at `c >= 1.0`, the prime-only determinant block is
strictly negative on 10/10 scanned widths (`R_prime` in `1.38 .. 2.06`) while
the arch and cross blocks flip positive for wide owners, so the obligation
splits into the robust prime-only sign plus a cross-domination inequality.
For fixed base/correction owners, the decay constants and
contraction threshold are formally selected before `n` and `lambda` (proof
record 1916); this does not close the joint margin. On the probed family the
vertex coefficient tracks `~0.5 * K^4` with the window factor `~7-10`, and the
relative determinant margin is thin (`5e-5 .. 0.116`), so any estimate route
to the determinant must be sharp (numeric branch discipline: law F79).

## Binding no-go results

- [002](002_one_shot_rh_route_verdict.md): ROOT endpoint positivity is not a
  universal B1 exit.
- [003](003_b1_b5_minimal_exit_route_selection.md): B1 globalization and the
  normalized additive B5 producer are frozen.
- [089](089_base_contraction_zero_target_no_go.md): unit-window geometric
  contraction is impossible on its stated owner.
- [093](093_widened_base_and_direct_absorption_nogo.md): widening the window
  does not repair that contraction scheme.

Do not reopen a no-go without changing a named hypothesis or owner and proving
why the obstruction no longer applies.

## Shared and fallback interfaces

- [001](001_first_cut_window_architecture.md) and
  [004](004_endpoint_literature_interface_audit.md): ROOT-local endpoint base.
- [007](007_b5_quantifier_repair_and_target_ladder.md): B5 quantifier and owner
  boundary.
- [009](009_n2beta_core_bone_completion_contract.md): retained legacy
  N2beta/window-taper contract because Lean source still cites it; inactive.
- [010](010_bone_foundry.md): obstruction-to-obligation workflow.
- [012](012_g8_same_owner_readback_rh_reachability.md),
  [027](027_r3_shifted_hardy_kernel_reduction.md),
  [042](042_g8_diagonal_leg_operator_bridge_audit.md), and
  [046](046_formal_leaves_after_1735.md): compressed G8/R3 fallback. This lane
  is not an active producer under the core-progress gate; its exact S3/B4
  obligations and shortcut no-gos are retained for possible reuse.

## Evidence labels

- `FORMAL`: checked Lean declaration; cite module, theorem, build log, and
  axiom audit where the record depends on it.
- `LITERATURE-BACKED`: a source has been checked, but the project has not
  formalized the result.
- `PAPER` or `PROJECT CANDIDATE`: internal derivation awaiting formal proof or
  an analytic estimate.
- `NO-GO`: scoped to the hypotheses and owner stated in the record.

Never promote a numerical observation directly into this map. Put unresolved
experiments in `docs/proofs/`.

## Compression ledger

The 2026-09-23 cleanup removed only superseded map narration:

- 005, 008, and 011: abandoned planning programs;
- 013--041 except 027: incremental R3/G8 construction records, consolidated
  into 012 and 042;
- 043--045 and 048--074: milestone/status ledgers, consolidated into the
  current producer records;
- 076--078 and 083--088: superseded physical/absorption subroutes;
- 095--102: generic conditional exits and tail wrappers frozen by the
  core-progress gate.

Mathematical and formal evidence remains in Lean, `docs/proofs/`, build logs,
and Git history. Deleted map files are recoverable with `git log --all --
docs/map/<file>` and `git show <commit>:docs/map/<file>`.

## Maintenance rule

Update an existing retained record in place. Add a new map file only for a
route-level decision that cannot be represented by one of the retained
authorities. Every update must state its owner, consumer, evidence level,
remaining open obligation, and whether it changes a route ruling. If it
changes binding route authority or current status, synchronize this index and
the project rules in the same change.
