# 035 — R3 actual-cutoff signed remainder limit

**Authority:** supporting.

**Status:** the same-owner signed finite-Euler remainder response has a formal
ordinary-trace limit under the actual source-compressed G8 cutoff. The limiting
remainder has not been shown to vanish; the full G8 readback to `qw` remains
open.

**Consumer:** the signed remainder leg of the actual G8 cutoff ledger on the
healthy `CompactLog` owner, downstream of which the active B5 consumer remains
`0 <= C1SameOwnerWeil.qw g` for the tower-selected detector.

Record [1480](../proofs/1480_r3_actual_cutoff_signed_remainder_limit.md)
transfers the existing same-owner first-jet pair and source-band pair
separately through the uniformly bounded doubled strong limit of the actual
cutoff. Subtracting the two convergent ordinary traces preserves the signed
remainder
`sourceActualBandFiniteEulerSoninResponse - sourceBandGramResponse`.

This proves convergence of that compressed source remainder term. It does not
prove decay to zero, identify it with the complete G8 readback remainder, or
close the survivor/boundary metric channels. `G8SameOwnerReadbackData`, the
trace-to-`qw` identification, the detector semi-local sign, C3, and RH remain
open. The binding route in [003](003_b1_b5_minimal_exit_route_selection.md)
does not change.
