# 037 — R3 actual-cutoff survivor/boundary channel pairing

**Authority:** supporting.

**Status:** the two ordered survivor/boundary mixed channels in the literal G8
metric ledger are formal adjoints at each finite cutoff, so their trace sum is
real and equals twice either orientation's real trace. Their cutoff limit is
still open.

**Consumer:** the mixed-channel portion of the actual G8 metric ledger on the
healthy `CompactLog` owner. The active B5 target remains
`0 <= C1SameOwnerWeil.qw g` for the tower-selected detector.

Record [1482](../proofs/1482_r3_actual_cutoff_survivor_boundary_pair.md)
proves the exact adjoint identity and finite-cutoff trace pairing. This is an
operator and scalar reduction only. It does not identify either channel with
the finite prime-power scalar or establish convergence. The
survivor-survivor and boundary-boundary limits, the mixed-pair limit, signed
remainder readback, `G8SameOwnerReadbackData`, detector semi-local positivity,
C3, and RH remain open. The binding route in
[003](003_b1_b5_minimal_exit_route_selection.md) is unchanged.
