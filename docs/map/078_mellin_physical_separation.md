# 078 — Mellin/physical separation no-go

Date: 2026-09-21.

Authority: formal supporting record under the binding B5 route in 003.

Record 1803 proves that finite Mellin node data do not determine a physical
prime-log sample on the genuine `CompactLogTest` owner. The theorem uses the
existing arbitrary finite Mellin interpolation plus one physical point, then
compares it with the zero test.

The active producer therefore needs an additional same-owner bridge from
Mellin interpolation to the finite bilateral profile. Existing consumers
already identify the exact required interfaces:

```text
PrimePairMatch / BilateralProfileMatchOn
    or
an explicit signed weighted profile identity
    or
a quantitative physical-kernel estimate.
```

This is a no-go for a shortcut, not a route no-go. The B5 signed budget and
RH remain open.

## Same-owner bridge brick landed (2026-09-21)

The first concrete bridge interface is now formal in
`ConnesWeilRH.Dev.C1P2OrbitPhysicalProfileReadback` (record 1804).  For an
actual `OrbitG8Geometry`, the selected half-density square is read back to
the explicit integral kernel of its raw base-power/correction factor; the
bilateral profile and the finite visible-prime aggregate are then rewritten
on that same owner.  The live gate is consequently equivalent to the
Archimedean term plus this explicit finite physical-kernel budget.

This is formal, axiom-clean, and only a representation identity.  It does
not supply the signed inequality, a Mellin-to-profile implication, or RH.

## Finite signed node certificate, 2026-09-21

The follow-up module `C1P2OrbitPhysicalProfileReadback` now proves two
consumer theorems. First, explicit signed upper bounds at every member of
the finite `orbitVisiblePrimeRange`, together with an Archimedean-plus-bound
sum at most zero, imply the same-owner orbit gate. Second,
`sourceRH_of_right_orbitGeometry_physicalKernel_nodeBounds` feeds that finite
certificate directly through the existing `SourceRH` consumer.

This is a formal interface tightening, not a sign proof. The node bounds may
be positive or negative, so cancellation is retained; no pointwise
nonpositive-profile shortcut is introduced. The raw `OrbitG8Geometry`
fields (`support_bound`, `visible_prime_cutoff`, zero control, and fourth-order
tail) do not themselves supply these bounds. The remaining producer is
therefore exactly the per-zero finite signed certificate on the actual
physical-kernel owner.
