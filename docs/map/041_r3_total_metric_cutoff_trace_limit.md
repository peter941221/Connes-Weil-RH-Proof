# 041 — R3 conditional total metric cutoff trace limit

Date: 2026-09-16.

**Authority:** supporting.

**Status:** the full actual four-channel G8 metric cutoff trace has a formal
limit under the same-basis square-summability assumptions for its two
diagonal detector-root legs. Those estimates remain open.

**Consumer:** the selected-owner G8 trace ledger for detector-specific
semi-local B5 positivity on a healthy `CompactLog` carrier; the binding route
in [003](003_b1_b5_minimal_exit_route_selection.md) is unchanged.

The paired Lean theorem is recorded in
[1486](../proofs/1486_r3_total_metric_cutoff_trace_limit.md). It combines the
conditional diagonal limits of [040](040_r3_diagonal_trace_limit_energy_constraint.md),
the survivor/boundary mixed-pair limit of
[038](038_r3_actual_cutoff_survivor_boundary_trace_limit.md), and the exact
four-channel physical metric trace identity. It does not construct either
root-column square-sum estimate or identify the limiting metric trace with
`qw`.

The signed endpoint remainder's vanishing and full readback identity,
endpoint/P2 sign, C3, and RH remain open.
