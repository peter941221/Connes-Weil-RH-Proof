# 1395 — 009 CLOSED as a construction/budget contract: final ledger and the honest map to RH

Date: 2026-09-13. Verdict up front: **GOOD — 009's obligation set is fully
discharged**. This record is the closing bookkeeping for
[009](../map/009_n2beta_core_bone_completion_contract.md): what each of its
five items ended as, what the contract explicitly never owned, and — because
the stated goal is now "打通 RH" — the exact remaining distance between
"009 done" and "RH proved", with nothing rounded off.

## 1. The 009 ledger, closed

```text
component 1  (L2 accessor + Lemma-A bound)             FORMAL DONE  (1381)
component 2a/2b/2c (Gram core + representers + tail)   FORMAL DONE  (1382-1384)
component 3  (smooth taper lift, (1+eps) K_loc)        FORMAL DONE  (1385)
component 4  (Young/convolution final-owner budget)    FORMAL DONE  (1386)
component 5 shape  (route-A margin consumer)           FORMAL DONE  (1387)
component 5 discharge (a)-(d)  R1 leaf, hfit           FORMAL DONE  (1391)
component 5 discharge (e)  (J1) digits, hJ1            RIG DONE, MODEL
     v2 VOID (1390->1392) -> v3 prereg (1393) -> VALID run + PASS region
     (1394) -> one-cell exact-rational certificate (1396)
component 5 discharge (f)  archimedean sign            NOT OWNED by 009
     (1389 s6: pre-existing since records 1080/1081; scale-invariant shape
     constraint; item 5 of 009 s5 = inherited open science)
```

009 section 2's five-item ladder ("build one smooth compactly supported
correction whose assembled healthy detector has an explicit L2 upper bound
close enough to the constrained Gram optimum to leave the N1c local-mass
budget strictly positive") is realized: the owner exists formally (1391),
the bound is closed-form and byte-matched to the consumer (1386/1387), and
the budget comparison the ladder demanded is now a gate-green MODEL fact
with a large-margin witness whose arithmetic survives exact-rational
enclosure (1394/1396). The contract's kill-or-live question — can route
alpha's budget arithmetic support a strictly positive local-mass margin? —
answers YES at MODEL level on a structured region of the locked grid.

## 2. What "009 done" does NOT include, stated flatly

1. **No Lean digit.** `hfit` and `hJ1` remain named hypotheses of
   `exists_routeAlphaOwner_margin_pos`; 1396's dyadics are meta-level
   evidence. Funding them in kernel arithmetic (`Nat.decLt`/NormNum-class
   evaluation over `Real.exp`/`Real.sinh` bounds) is a NEW formal obligation
   009 never listed — if pursued it belongs to the wiring record it enables,
   not here.
2. **No detector data.** `HealthyYoshidaDetectorData rho g` still needs the
   (f) sign; the 1391 wiring theorem is conditional on exactly that.
3. **No RH inference in either direction** (law 65 + every prereg's
   interpretation lock). FAIL cells are real telemetry constraining future
   window choices; PASS cells are budget arithmetic.

## 3. The remaining distance to RH — the whole tower, one table

From the live frontier card and record 1343's machine-checked equivalence
(`weilCriterion_iff_sourceRH`, B0b brick):

```text
+----+---------------------------------------------------+---------------+
| #  | rung                                            | status        |
+----+---------------------------------------------------+---------------+
| 1  | hypothetical off-line zero rho -> R1 owner g     | FORMAL (1391) |
|    |   with values, pinning, budget (items a-d)       |               |
| 2  | (J1) at a chosen witness cell (item e)           | MODEL+CERT    |
|    |   (1394 PASS region; 1396 exact rational at 1    | (this wave)   |
|    |   cell)                                          |               |
| 3  | archimedean sign (item f):                       | THE OPEN      |
|    |   0 < archimedeanTerm g.convolutionSquare        | SCIENCE, (a)  |
|    |   for a budget-fitting shape                     |               |
| 4  | owner carries HealthyYoshidaDetectorData         | FORMAL,       |
|    |   (1389 IFF: exactly rung 3) + qw(g) < 0 tower   | conditional   |
|    |                                                  | on rung 3     |
| 5  | Weil criterion: 0 <= qw(h) for every healthy     | THE WALL —    |
|    |   CompactLog test (machine-checked iff SourceRH  | classical     |
|    |   via 1343/B0b)                                  | content of RH |
| 6  | N3/N4 adapters: zero counting + total signed     | OPEN, mapped  |
|    |   budget + low-height remainder (008 domain,     | in 008, not   |
|    |   consumes rungs 4+5)                            | 009           |
+----+---------------------------------------------------+---------------+
```

Two honest readings of this table:

* Rung 3 is now a **concrete, computable sign on a constructed shape**: the
  functional is the explicit formula
  `archimedeanTerm F = (log 4pi + gamma) F(0) + int_0^inf [e^(y/2)(F(y)+F(-y))
  - 2F(0)] / (2 sinh y) dy` (`C1SameOwnerWeil.lean:48-64`,
  `SelectedWeilSquareOwner.archimedeanDenominator y = e^y - e^{-y}`), and the
  1391 owner `g = u.convolution f` is an explicit solved-system taper — so
  the sign is numerically evaluable at every cell that passed (J1), and the
  campaign has a next rig. This was not true at the 1387 lock-in, when the
  owner did not exist.
* Rung 5 is not a task: it is equivalent to RH itself (B0b). "打通 RH" through
  this tower means rung 5 gets a PROOF; rungs 3/4/6 are the campaign's
  engineering, and 009's was rungs 1-2. History (1087-1227 waves, the COLD
  thermometer, the rank-3 no-go) says rung 5 resists estimation methods;
  whatever closes it will have to be structural, not a bigger grid.

## 4. Register actions taken with this record

* 009 header gains a CLOSED banner (obligation set discharged; the two
  remaining OPEN lines in its section 4 are, by the contract's own words,
  inherited/not-owned science).
* map README gains item 27 (this wave: 1395 + 1396).
* Next executable surface moves to 008's total-variant frame: the rung-3
  archimedean-sign rig on the route-alpha owner is the natural candidate
  (its prereg would be 1397+ under the 1373 protocol — model formulas
  committed before any sign digit; scope per 009 s3, no wider).

RH not claimed anywhere in this record; no gate sign computed here.
