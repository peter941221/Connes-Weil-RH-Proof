# 1265 — G8 physical cutoff metric/internal split

Date: 2026-09-10.

Status: FORMAL OWNER-LEVEL ALGEBRA. This brick serves the healthy-
`CompactLog`, detector-specific B5 consumer. It adds no generic interface,
no numerical premise, no `qw` sign, and no RH conclusion.

## Result

For the physical endpoint finite-cutoff pair, let

```text
  C_n = (sourceInclusion lambda)†
          ∘L (g8SourceCutoffPairData ... n).left.
```

The new theorem
`g8PhysicalEndpointSourceCutoffPairData_traceProduct_eq_metric_add_internal`
proves the exact same-owner identity

```text
  physicalTraceProduct_n
    = C_n† (M† W_g M) C_n
      + C_n† K_forward C_n,
```

where `M = finiteEulerMetricCoframe` and

```text
  K_forward = F† W_g M + M† W_g F + F† W_g F.
```

The first term is the metric Gram already exposed by the G8 survivor/boundary
ledger. The second is the internal correction generated by the physical
endpoint `E = F + M`. The equality is proved before any trace limit is
introduced, so the correction remains inside the positive kernel.

## Why this is substantive

Records 1258–1263 left the physical endpoint and the existing G8 cutoff
sequence as distinct carriers. This theorem does not assert that they are the
same; it gives the physical carrier's exact finite-cutoff decomposition with
the literal cutoff leg. It therefore supplies the first algebraic equation on
which a future finite-visible-prime comparison can operate.

The theorem does not yet identify either summand with the finite Euler
boundary trace. The next mathematical target remains the carrier-alignment /
finite-prime comparison (G8-P0/P1 in record 1264), followed by a proved
zero-limit residual.

## Verification

Owning module and paired audit:
`ConnesWeilRH.Dev.C1G8AdjointShearGram` and
`ConnesWeilRH.Dev.C1G8AdjointShearGramAudit`.

Evidence: `1264_g8_physical_cutoff_metric_split_retry.log` reports
`Build completed successfully (3922 jobs)`, zero `error:` lines, zero
`sorryAx`, and the new declaration has exactly
`[propext, Classical.choice, Quot.sound]`.

RH is not claimed.
