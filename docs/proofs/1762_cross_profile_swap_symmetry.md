# 1762 — Directed cross profile swap symmetry

Date: 2026-09-21

The theorem `bilateralProfile_pairTest_swap_re` proves that the pair tests
`f* * g` and `g* * f` have the same real bilateral profile at every real
coordinate. The proof uses the exact reflection-conjugation identity for
additive convolution and translation invariance of the real-line integral.

Therefore the AB and BA finite-prime profile channels from record 1761 are
equal pointwise in their real observable. This is an arithmetic reduction
only; the Archimedean cross contribution and the final signed inequality
remain open.

Verification: `shortest_route_20260921_swap_profile_v20.log`, build
completed successfully (3784 jobs), zero `error:` lines, zero `sorryAx`, and
standard three-axiom Audit output.
