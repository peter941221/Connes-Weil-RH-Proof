# 1761 — Two-span four-channel profile expansion

Date: 2026-09-21

The theorem `signedProfileTerm_twoSpan_eq_four_pair_profiles` expands one
finite visible-prime profile term of
`spanObj [A,B] [1,-lam]` into the four pair profiles AA, AB, BA, and BB.
The cross channels retain their direction and are weighted independently by
`-lam`; the reference square is weighted by `lam^2`.

The result is exact algebra on the same CompactLog owner. It supplies no sign,
numerical estimate, prior-art claim, or RH conclusion.

Verification: `shortest_route_20260921_four_profile_v15.log`, build completed
successfully (3784 jobs), zero `error:` lines, zero `sorryAx`, and standard
three-axiom Audit output.
