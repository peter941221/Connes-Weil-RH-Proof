# 1654 - Off-diagonal Hardy-column consumer for S3

Date: 2026-09-19.

Status: formal conditional consumer; the off-diagonal column estimate remains
open. RH is not claimed.

The factor identity from 1653 is now consumed quantitatively: if the columns
of `(I-P) K_b P` are square-summable on a named finite-S basis, then bounded
postcomposition by its adjoint gives square-summable columns for
`P - P K_b P K_b P`. Records 1652 and 1651 then transport this to the actual
finite-S leakage leg.

Thus the live S3 analytic producer has the standard form of one off-diagonal
Hankel column estimate. No positivity-to-HS shortcut, trace regularization,
detector sign, or RH premise is used.
