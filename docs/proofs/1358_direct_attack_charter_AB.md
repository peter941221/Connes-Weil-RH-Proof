# Record 1358 (CHARTER) - DIRECT ATTACK on the gate via A (repulsion) and B (dictionary)

```text
+---------------------------------------------------------------------+
| Owner order 2026-09-12: "A B 怎么打？直接打！RH打成了，不许给我其他   |
| 结果." Accepted as: BOTH routes open as MAIN effort (no menu); the  |
| defined STOP condition for "打成了" is ONE thing only: a Lean        |
| certificate discharging the machine-checked gate (weilCriterion,    |
| d767a1d) - i.e. a proof object, not a probe, not a spec, not a      |
| plateau. Every artifact short of that ships labeled as waypoint,    |
| never as the result. This charter is the commitment, not the proof. |
| RH NOT claimed at any point in this file. MODEL grade where noted.  |
+---------------------------------------------------------------------+
```

## 1. The battle geometry (what "the gap" is, in one formula)

From committed budgets only (1346 s2 factor-4/factor-2 algebra +
1353 placement audit), the contest balance B4.1 (machine-checked,
275aca6) specializes to the SUFFICIENT local condition

    A * N_on(I)  >=  2 * N_off(I)     for every Nyquist window I,

i.e. near-line off-line fraction f(I) < f*(A) with

    f*(A) := A / (A + 2)

where A is the windowwise spread/frame ratio: A = 1 for perfectly
delocalized on-line sampling nodes (1346 s2 sanity: f* = 1/3, matches
the locked constant), and A ~ 1/70 - 1/146 for the cluster
configurations 1353 built from the prolate information ceiling
sigma|J|/pi. The NLLE-v2 theorem (1353 s5) IS the statement that the
real zero set has A bounded away from 0 jointly with f < f*(A).

Consequence (why this is attackable): the gap is now a PAIR OF
EXPLICIT NUMBERS (A_min, f*), not a fog. The enemy is one theorem:

    TARGET-A:  exists explicit A0 > 0 such that every window at
    height >= T0 has on-line spread ratio >= A0   [limb (ii)]
    plus the density half  [limb (i)].

## 2. Wave W1 (A route, MAIN): the thermometer becomes combat - "A_tech"

Single question, explicit: **what is the largest A0 any UNCONDITIONAL
technology currently located in-repo or in the 2026 literature
certifies?** This is task #9 promoted to main effort. Deliverables:

  W1.1  S(T) primary-parse (Trudgian-line constants + the OPEN
        T-range question): decides whether any window-totality bound
        exists at ALL heights >= T0. (Occupancy band [0.50,0.94]logT
        becomes measured, replacing PLACEHOLDER.)
  W1.2  Spread inventory: for each located repulsion-flavored result
        (Montgomery support-1 under RH; Rudnick-Sarnak; Bourgade
        log-scale; GGOS/Chirre on-line gaps; DH near Re=1; 2206.11729
        half-isolation), extract the window-level lower bound on
        sum |G(iy_j)|^2 / energy, i.e. its certified A - under RH or
        unconditional, labeled.
  W1.3  Compute f*(A_tech) and the RESIDUAL f(tech) available from
        density tech; issue the locked verdict table:

        OUTCOME 1: A_tech * (density-feasible N_on) >= 2 * (density-
                   worst-case N_off) on all windows => limb (i)+(ii)
                   from EXISTING tech, gate discharges from the
                   machine-checked brick chain => direct attack
                   succeeds modulo writing the windowwise brick
                   (B2/B3, weeks, no invention) - THE WIN PATH.
        OUTCOME 2: positive but insufficient A_tech => publish target
                   theorem "NLLE needs spread >= A_needed - A_tech":
                   the gap becomes a named, constant-bearing open
                   problem (H2 hardened), still a genuine result but
                   NOT the stop condition.
        OUTCOME 3: A_tech = 0 (expected modal outcome: NO uniform
                   unconditional on-line spread bound is currently
                   known) => A-route main line becomes: PROVE
                   TARGET-A as a new theorem; W1 output = its
                   precise statement + the exact 1/log-scale scale
                   where it must bite. Attack continues, on the hill.

  Prereg discipline: W1.3 bands are LOCKED BEFORE any number is
  computed (law 42), in the W1 probe prereg (next record number at
  its creation).

## 3. Wave W0 (B route, SIMULTANEOUS, paper+source only): the Connes dictionary

Connes 1999 (arXiv:math/9811068): RH <=> Weil positivity on his
adelic test class, with the absorption-spectrum interpretation. Our
tower: SourceRH <=> gate on CompactLogTest vanishing class (d767a1d,
B4 restatement). Both sides exist; the CLASSES differ. Deliverable:
a gap certificate answering exactly:

  W0.1  his test class -> our side: which of his test functions
        restrict to our compact-log owners (finite-prime
        truncation = our support rule?) - list identities;
  W0.2  our side -> his: does our vanishing class sit inside a
        subclass where his positivity machinery already proves
        something (e.g. his "eventual noncritical zeros" section)?
  W0.3  RESIDUAL: the single construction whose existence would move
        positivity across the gap = the SPECIFICATION of the object
        Species B must invent. Output: "B-blueprint v1" record.

B route stop condition, stated plainly: a self-adjointness/positivity
certificate for that specified object, checked at the gate. Until
then W0 output is a blueprint, never a result.

## 4. What "RH 打成了" means operationally (owner's stop word)

Definition, fixed: the sentence is earned iff a Lean artifact
discharges `∀ g vanishing, 0 <= qw g` (or equivalently the
contestForm of 1356 B4.2) from axioms, green log, accepted by footer
rule. Nothing else counts: not a plateau (1357), not a spec (1353),
not a blueprint (W0), not a temperature (W1). W1 OUTCOME 1 is the
only identified branch in which the stop word can be earned inside
this campaign's lifetime from existing pieces + B2/B3 engineering.
O3 (expected) means the direct attack becomes the open theorem
TARGET-A: we attack it anyway, and every rung reports distance.

## 5. Ledger discipline under a direct-attack order

- No wording inflation: waypoints ship labeled waypoint (this is the
  anti-1352 lesson generalized; the register-label trap).
- Probes: SPENT accounting continues; W1 preregs before digits.
- Memory: every wave updates the cards the same day.
- The owner's "不许其他结果" is honored by the DEFINITION of the
  result (s4), not by silence about distances: distances are how
  sieges are run.
