# Proof 684: trace consumer for the signed radial pair-data owner

Proof 684 consumes Proof 683's two physical pair-data premises and proves:

```text
IsTraceClassAlong sourceBasis (radialSignedPhysicalOwner p S)
IsCompactOperator (radialSignedPhysicalOwner p S)
```

The compactness statement requires a Hilbert basis for the packed target
`WithLp 2 (K × G)`.  The root-producer specialization also reads the same
legality through arbitrary bounded same-domain left/right maps while keeping
the signed owner intact.

This closes only a conditional Gate 3L/readout interface.  It does not give a
finite-S-uniform pair energy, a radial boundary factorization, Gate 3U, the
finite-S sign, Burnol's identity, or RH.
