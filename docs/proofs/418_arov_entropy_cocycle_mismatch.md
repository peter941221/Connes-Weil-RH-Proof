# Proof 418: Arov entropy and paired-cocycle mismatch

Date: 2026-07-19

Status: exact analytic rejection of a direct Arov--Krein entropy estimate for
Proof 400's normalized local trace cocycle.  In the literal two-dimensional
Schur frame from Proof 402, the canonical one-factor Euler/Szego energy is
quadratic in the Euler amplitude, while the fully paired and scalar-normalized
detector response has a nonzero linear term.  The forward and inverse
contractions also cannot be the two diagonal blocks of one scalar lossless
node.

This closes only the direct-import lane.  It does not reject a new
Burnol-specific localized entropy which first recombines the complete
outer-minus-Sonin/prolate boundary object.  Gate 3U, the finite-`S` sign,
Burnol's identity, and RH remain open.

## 1. Result

```text
+------------------------------------------------------+---------------------------+
| layer                                                | judgment                  |
+------------------------------------------------------+---------------------------+
| literal Proof 390 Schur parameter                    | `Phi_a=a`                 |
| one-factor Euler/Szego energy                        | `O(ell a^2)`              |
| paired normalized trace cocycle                      | `2ca+O(a^3)`              |
| odd term after `G R=rho I` normalization             | survives exactly          |
| direct energy-to-cocycle estimate                    | false                     |
| `G,R` as diagonals of one scalar lossless node       | impossible                |
| source-specific completed localized entropy          | still open                |
| Gate 3U / finite-S sign / Burnol / RH                 | open / open / open / open |
+------------------------------------------------------+---------------------------+
```

The decisive scale comparison is

```text
q_a=2ca/(1+a^2),
I_a=-log(1-a^2),
E_(ell,a)=ell I_a,

q_a/I_a -> infinity,
q_a/sqrt(I_a) -> 2c,

a=exp(-ell/2)
  ->q_a/[(1+ell)^d E_(ell,a)] ->infinity
    for every fixed d.                                (AE.1)
```

Thus the positive entropy can support only a square-root estimate at this
level.  It does not turn the local `p^(-1/2)` response into the canonical
`p^(-1)` Euler energy.

## 2. Literal physical Schur frame

Work on `C^2` with

```text
U=[0 1; 1 0],
P=diag(1,0),
W=I+cU,                 0<c<1,
T_a=I-aU,               0<a<1.                       (AE.2)
```

Then `U` is unitary and self-adjoint, `W` is positive, and `[W,U]=0`.  The
transported source line is generated by

```text
v_a=(1,a)/sqrt(1+a^2),
P_a=v_a v_a*.                                         (AE.3)
```

The exact detector response is Proof 402's value

```text
q_a=Tr[W(P_a-P)]=2ca/(1+a^2).                         (AE.4)
```

This is not an abstract contraction example.  It is the literal specialization
of Proof 390.  In the block decomposition for `P direct-sum (I-P)`, one has

```text
U_00=U_11=0,
U_01=U_10=1,
X_a=a,
Phi_a=U_00+U_01 X_a=a.                               (AE.5)
```

The graph cosine and Schur frame are

```text
C_a=(1+a^2)^(-1/2),
Z_a=(1-a Phi_a)C_a
   =(1-a^2)/sqrt(1+a^2).                              (AE.6)
```

## 3. Complete forward/inverse pairing does not remove the odd term

Proofs 395--400 normalize the one-step frames by

```text
G_a=Z_a/(1+a)
   =(1-a)/sqrt(1+a^2),

R_a=(1-a)Z_a^(-1)
   =sqrt(1+a^2)/(1+a),

rho_a=(1-a)/(1+a).                                    (AE.7)
```

Hence

```text
G_a R_a=R_a G_a=rho_a.                                (AE.8)
```

Let

```text
alpha_0=<e_1,W e_1>=1,
alpha_1=<v_a,W v_a>=1+2ca/(1+a^2).                    (AE.9)
```

The complete local relative defect from Proof 396 is

```text
f_a=G_a alpha_1 R_a-rho_a alpha_0
   =rho_a q_a.                                        (AE.10)
```

Proof 400 divides by the same scalar.  Therefore

```text
rho_a^(-1)Tr(f_a)=q_a=2ca/(1+a^2).                    (AE.11)
```

The scalar gauge cancellation is exact, but the physical first variation is
still odd and nonzero.  A proposed paired entropy may not claim that
`G_aR_a=rho_a` deletes the half-power term.

## 4. The canonical energy scale

The direct proposal must connect the physical cocycle to Proof 416's actual
one-factor diagonal Euler energy.  For a factor with amplitude `a` and positive
frequency `ell`, that charge is

```text
E_(ell,a)
 =ell sum_(m>=1) a^(2m)/m
 =ell[-log(1-a^2)].                                    (AE.12)
```

This formula comes from the literal Euler logarithm; it does not assume that
Proof 390's transfer value `Phi_a` has already been identified with a
Verblunsky coefficient of an Arov canonical system.  For the route values

```text
a=p^(-1/2),
ell=log(p),                                           (AE.13)
```

equation `(AE.12)` is exactly the single-prime summand of Proof 416's canonical
energy.  Put `I_a=-log(1-a^2)=a^2+O(a^4)` for its unweighted charge.

The continuous Arov-gauge theorem has the same quadratic anisotropy structure:

```text
I(w)=integral_0^infinity
  [tr A(t)-2 sqrt(det A(t))] dt.                      (AE.14)
```

For eigenvalues `lambda_1,lambda_2`, its integrand is

```text
(sqrt(lambda_1)-sqrt(lambda_2))^2.                    (AE.15)
```

Combining `(AE.4)` and `(AE.12)` first gives the unweighted mismatch

```text
q_a/I_a
 =2ca/[(1+a^2)(-log(1-a^2))]
 ~2c/a ->infinity.                                    (AE.16)
```

Consequently no constant independent of `a` can satisfy

```text
abs(rho_a^(-1)Tr(f_a))<=C I_a.                        (AE.17)
```

More strongly, let `a=exp(-ell/2)`.  For every fixed polynomial support loss,

```text
q_a/[(1+ell)^d E_(ell,a)]
 ~2c exp(ell/2)/[ell(1+ell)^d]
 ->infinity.                                           (AE.18)
```

Thus inserting the frequency weight from the canonical energy does not repair
the local estimate.  The sharp generic scale available from the unweighted
positive charge is instead

```text
q_a/sqrt(I_a)->2c.                                    (AE.19)
```

Equation `(AE.19)` agrees with Proof 417's whole-tilt Cauchy--Schwarz bound.
It does not provide a new summable prime estimate.

## 5. The two contractions are not one lossless node

A direct Redheffer interpretation would need an actual unitary colligation,
not merely two contractions whose product is scalar.  Already in the scalar
example, `G_a` and `R_a` cannot be the two diagonal blocks of one `2 x 2`
lossless node.

Indeed, if

```text
mathcalU=[G_a b; c_0 -R_a]
```

were unitary, comparison of its row and column norms would force

```text
G_a^2=R_a^2.                                          (AE.20)
```

But `(AE.7)` gives

```text
R_a^2-G_a^2
 =4a^2/[(1+a)^2(1+a^2)]>0.                           (AE.21)
```

This does not prove that no larger source-specific unitary dilation can ever
be constructed.  It proves that Proof 395's pair is not itself the diagonal
of the lossless node required for an immediate Arov/Redheffer chain rule.
Defect spaces and the physical outer/second-support/prolate channels would
have to be constructed and identified separately.

## 6. Why alternative local charges do not repair the scale

The forward and inverse frame defects are

```text
1-G_a^2=2a/(1+a^2),
1-R_a^2=2a/(1+a)^2.                                  (AE.22)
```

They are linear in `a`.  A positive node energy built directly from those
defects can see `(AE.11)`, but its prime ledger has the forbidden scale

```text
sum_p p^(-1/2).                                       (AE.23)
```

The canonical Euler/Szego charge `(AE.12)` has the desired quadratic ledger,
but it controls the response only after a square root.  The direct Arov proposal
therefore faces an exact dichotomy:

```text
physical frame-defect charge -> right observable, wrong half-power budget;
Euler/Szego energy            -> right quadratic budget, wrong linear control.
                                                               (AE.24)
```

Only a source identity which cancels the complete first variation before the
positive majorant can bridge `(AE.24)`.

## 7. Source audit

The primary Arov-gauge source states a sum rule for the spectral Schur
function of a two-dimensional canonical system:

```text
David Damanik, Benjamin Eichinger, Peter Yuditskii,
Szego's Theorem for Canonical Systems: the Arov Gauge and a Sum Rule,
Theorem 2.1 and Appendix B,
https://arxiv.org/abs/1907.03267
https://arxiv.org/pdf/1907.03267
```

Theorem 2.1 is exactly `(AE.14)`.  Appendix B explains unitary nodes and the
Redheffer transform.  It does not identify Proof 395's infinite-dimensional
source contractions `G_j,R_j` with one unitary node, and it does not state a
detector-gradient estimate for Proof 415's weighted Burnol boundary form.

The source claim and the project inference must therefore remain separate:

```text
source theorem:
  spectral entropy = positive canonical-system coefficient integral;

project obligation:
  completed Burnol detector cocycle
    <= polynomial of the canonical Euler square energy.          (AE.25)
```

## 8. Reproducible certificate

The companion script checks `(AE.3)--(AE.12)`, the normalized cocycle
readback, the entropy ratios, and the lossless-diagonal obstruction:

```text
OPENBLAS_NUM_THREADS=1 python3 -B \
  docs/proofs/418_arov_entropy_cocycle_mismatch_probe.py
```

The default WSL2 cohort reports

```text
maximum response error       2.220446049250e-16
maximum pair error           0
maximum cocycle error        5.447031714567e-16
maximum node-gap error       5.477389375397e-16
growth of q/I_a              1.697797016321e1
final q/sqrt(I_a) error      1.562270137920e-4
```

The matrices verify the algebra.  The rejection of `(AE.17)--(AE.18)` is
analytic from the displayed limits, not a floating-point extrapolation.

## 9. Correct successor

Do not attach the canonical energy directly to the local defects in Proof 400.
The surviving target is still Proof 416 `(EN.7)`, or a positive localized
completion satisfying Proof 417 `(ET.36)--(ET.37)`, with this stricter order:

```text
complete weighted Burnol boundary form
  -> outer-minus-Sonin/prolate first-variation cancellation
  -> positive localized quantity
  -> canonical O((1+B_F)^2) Euler energy
  -> one absolute value.                              (AE.26)
```

Any proposed Arov/Redheffer construction must first prove that its variation
is Proof 400's same signed cocycle and pass `(AE.11)`.  If its local positive
charge is even and quadratic in `a`, it must exhibit the additional
source-specific cancellation rather than assert an `O(a^2)` local response.

## 10. Decision

```text
direct Arov sum-rule import:                 rejected by `(AE.16)--(AE.18)`;
odd-term cancellation from `G R=rho I`:      false by `(AE.10)--(AE.11)`;
`G,R` as one scalar lossless-node diagonal:  false by `(AE.21)`;
square-root entropy scale:                   exact and already Proof 417;
Burnol-specific localized completion:        open;
Gate 3U / finite-S sign / Burnol / RH:        open / open / open / unproved.
```
