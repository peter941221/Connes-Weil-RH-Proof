# 1757 — One-window defect as a genuine two-span

Date: 2026-09-21

The exact identity
`oneWindowICdefect_eq_twoSpan` proves that the Stage-B one-window defect
`ICdefect g {()} (fun _ => W) (fun _ => lam)` is the genuine two-dimensional
`CompactLogTest` span with basis `[g, W]` and coefficients `[1, -lam]`.

This connects the existing defect-control route to the finite gate-matrix
socket without changing the owner or hiding the signed subtraction. It proves
no defect bound, sign, or RH statement: the independent defect budget and the
reference negative-gate certificate remain required.

Verification: `shortest_route_20260921_defect_span_v10.log`, 3784 jobs, zero
`error:` lines, zero `sorryAx`, and standard three-axiom Audit output.
