# Record 1706 — survivor energy reaches the source-projection endpoint

The new formal consumers

`g8SurvivorCoframe_energy_iff_hardyCompressedRootEnergy`

and

`g8SurvivorCoframe_energy_iff_sourceProjectionRootEnergy`

compose the 1705 bare source-compressed normal form with the existing Gate
normal-form theorems.  For every healthy-owner root, finite visible-prime
family, Sonin scale, and named source basis, the survivor coframe square-sum
is equivalent to the square-sum of

`sourceSoninProjection lambda ∘L rootConvolution owner ∘L sourceInclusion lambda`.

The Hardy-compressed form is an intermediate equivalent: its prolate
remainder is already square-summable and therefore contributes no remaining
analytic obligation.

This is a formal endpoint reduction only.  It does not prove the projected
root square-sum, close S3, or imply RH.
