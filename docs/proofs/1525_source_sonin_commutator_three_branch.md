# Source Sonin commutator inherits the three-branch owner

Date: 2026-09-17

The theorem `sourceSoninCommutator_eq_threeBranch` instantiates the committed
identity `R = E Q E - K` for the actual source Sonin projection.  For every
ambient factor `M`, its commutator with `P` is exactly the signed sum of the
outer, second-support, reflected-outer, and prolate branches, with `M` kept in
the ambient slot.

This gives the B4 atomic target the same branch owner already used by the
moving-scale detector-root analysis.  The identity is purely algebraic and
does not claim that any branch is Hilbert--Schmidt; the branch estimates and
the B4/S3/G8/C3 exits remain open.

Validation: owning build log
`/home/peter/rh/build-logs/1672_sonin_three_branch.log` completed with zero
`error:` and zero `sorryAx`; the paired audit declaration uses only the
standard three axioms.
