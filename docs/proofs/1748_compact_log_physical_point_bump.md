# 1748 - Compact-log physical point bump

Date: 2026-09-20.

Status: FORMAL physical interpolation primitive; no sign claim.

The P2 prime terms sample the actual compact-log test at physical points
`log n`. Record 1748 ports the existing interval bump construction from the
normalized owner to the genuine `CompactLogTest` owner:

`a < x < b` implies a compact smooth test supported in `[a,b]` with value
one at `x`.

This is the first physical-coordinate input for a future finite bilateral
profile interpolation. It does not preserve the healthy Mellin target values,
does not prove an aggregate sign, and does not imply RH. The required next
brick is finite-point interpolation subject to the triple Mellin constraints,
not a substitution of Mellin-node data for point values.

Evidence: `C1P2PhysicalPointBump.lean` and its paired Audit;
`build-logs/shortest_route_20260920_physical_bump_v2.log` reports 2917
successful jobs, zero `error:` lines, zero `sorryAx`, and standard axioms only.
