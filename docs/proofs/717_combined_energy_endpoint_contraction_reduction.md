# Proof 717: Combined Energy Endpoint-Contraction Reduction

Proof 717 reduces the unresolved `hcombined` source theorem to one exact
operator statement:

```text
||sourceActualBandForwardEndpointCoframe lambda family|| <= 1
```

Given that contraction, the existing `boundedPrecomp` Hilbert--Schmidt ledger
and the fixed physical majorant prove:

```text
sourceActualBandCombinedPhysicalRightEnergy
  <= fixedPhysicalEnergyMajorant
```

The reduction keeps the complete endpoint coframe intact. It does not split
the forward, metric, outer, reflected, second-support, or prolate channels,
and it does not claim the endpoint contraction itself. That contraction is now
the exact next producer target for Gate 3U.
