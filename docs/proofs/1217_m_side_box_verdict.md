# Record 1217 - M-side box certificate VERDICT (prereg 1215 D5, numeric layer)

Date: 2026-09-07.  Status: LANDED.  Prereg:
1217_m_side_box_certificate_preregistration.md (committed a32e53e
before any run; amendments sec. 6/7/8 each committed BEFORE the next
rerun, law 42).  Consumers: records 1215 (D5), 1216 (revised repair).
No P2, no SourceRH, no RH.

## 1. Verdict

The TRUE Lean gate matrix on the class (2,8) owner is certified
entrywise by rational boxes:

```text
mixed parity  (i + j odd)    EXACT [0, 0], owned by the Lean parity
                             theorem gateMatrix_zero_of_odd (D1,
                             C1GateMatrixParity, landed 05bb009);
                             numerics cross-check |mid| <= 7.4e-47
same parity  (i + j even)    rational boxes on the dyadic grid with
                             denominator 2^72, worst certified width
                             2.395e-13  (deliverable <= 1e-9),
                             entry budgets 1.118e-13 .. 1.198e-13
                             (gate 4e-12, cap 5e-10)
```

Falsifier (b) PASSED: with the whitening ported verbatim from the
1216 probe (3-scale moment kernel, dim V = 5, self-check reproduces
cert top_mid to 4.9e-16),

```text
top(M_mid | V whitened)   = -1.443377e-06
inflation radius          = +1.015e-08
top + inflation           = -1.433227e-06
required                  <= U - 1e-8 = -1.053377e-06
verdict                   FALSIFIER-B PASS  (margin factor ~38)
```

so the boxes support the margin consumption: the true gate matrix's
whitened top on V is below U = -1.043377e-06 with room to spare.

Regression cross-check: the engine's same-parity midpoints reproduce
the committed 1112 M_mid to worst 1.405e-12 - independently matching
the 1.4e-12 deviation measured by the 1216 probe (both trace to the
quadrature scale, far under the 5e-9 check).

## 2. Run ledger (five official launches, three retired with evidence)

```text
run 1  exit 5   flat per-point budget abort -> prereg sec. 6
                amendment (a-posteriori perr, floor 1e-13)
run 2  stopped  arch domain slip (0,8) vs (0,4), caught by the
                vs-1112 regression check -> fixed; the check is now
                a permanent emitter validation
run 3  stopped  widths 7.1e-12 deterministically fail falsifier (b)
                (inflation 6.43e-07 vs allowed 3.90e-07; whitening
                replay self-check 4.9e-16) -> prereg sec. 7 (grid
                16x400 + 4e-12 gate); checkpoint archived as
                1217_m_boxes_checkpoint_run3.json
run 4  exit 7   sec. 7 gate fired at 7.53e-12 vs 7.12e-12 at 16x200:
                the budget RISES under refinement -> floor artifact;
                the diagnostic 1217_budget_diagnostic.py measured
                half-rule deltas 8.9e-17 and budget @ floor 1e-13 =
                7.1175e-12 (reproduces run 3 exactly) -> prereg
                sec. 8 (floor 1e-15, grids back to 16x200)
run 5  exit 0   LANDED: 64/64 entries, no gate or budget failures,
                WROTE cert JSON
```

No hand-narrowing anywhere; every retirement kept its log and
checkpoint as evidence.

## 3. As-run engine (sec. 3 base + sec. 6/8 amendments)

```text
precision        mpmath dps = 45
inner integral   composite GL, 16 panels x 200 nodes on (-2, 2),
                 half rule 16x100; measured |GL_n - GL_{n/2}|
                 <= 8.9e-17 at every required point (gate 1e-12)
arch integral    composite GL, 16 panels x 200 nodes on (0, 4),
                 half rule 16x100; measured delta <= 1.7e-17;
                 single-expression integrand through y = 0
constants        log(4pi)+gamma, log(tanh 2) enclosed at +/-1e-14
per-point perr   max(2 x measured half-rule delta, 1e-15)
entry budget     accumulated from perr charges, gate 4e-12 (fired
                 never), cap 5e-10
rounding         outward dyadic, denominator 2^72
```

## 4. Landed artifacts

```text
docs/proofs/1217_m_boxes_cert.json        64 rational boxes +
                                          budgets + gates + the
                                          whitened_check block
ConnesWeilRH/Dev/C1GateMatrixBoxData.lean GENERATED by
                                          1217_emit_box_data.py:
                                          MLo_q28M / MHi_q28M,
                                          zero_box_odd_data,
                                          gateMatrix_mem_zero_box_of_odd,
                                          q28_hboxM_of_sameParity
                                          (conditional binding)
ConnesWeilRH/Dev/C1GateMatrixBoxDataAudit.lean   axioms + examples
docs/proofs/1217_budget_diagnostic.py     sec. 8 evidence
```

## 5. Honest scoping

The same-parity entrywise containment (20 independent entries) is
NOT discharged in Lean: it is consumed as the named hypothesis of
q28_hboxM_of_sameParity.  Discharging it is the registered
follow-up campaign (rational envelope machinery on the shifted
correlations, the 1131-1139-scale construction).  The mixed-parity
entries ARE discharged now by D1.  RH NOT claimed.
