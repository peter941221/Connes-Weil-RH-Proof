# 1617 — One-sided Hardy-corner reduction of the source root

Date: 2026-09-18

## Result

The source inclusion satisfies `J† P = J†`, so the live source-compressed
root has the sharper identity

`J† C J = J† (E Q E) C J - J† R C J`,

where `P = E Q E - R`. This avoids introducing a second Hardy corner on the
input side. The first term is exactly the adjoint pullback of the existing
Hardy-compressed root energy interface; the second contains only the prolate
remainder and is the bounded postcomposition target for the existing
prolate-factor square-summability results.

This is a formal reduction, not the missing S3 estimate. The central
Hardy-corner producer and the B4 Fourier-defect producer remain open.

## Acceptance

Build log: `/home/peter/rh/build-logs/1617_source_compressed_kernel_one_sided_retry3.log`.

The paired implementation and audit built successfully in 3957 jobs, with
zero `error:` lines, zero `sorryAx` occurrences, and four standard
`Quot.sound` audit markers.
