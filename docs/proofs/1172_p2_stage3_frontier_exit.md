# Record 1172 — P2 Stage-3 frontier exit

Date: 2026-09-06

## Result

`sourceRH_of_healthyDetector_p2Stage3RemainderFacts` packages the two named
Stage-3 facts for each right-oriented off-line detector:

1. Hilbert–Schmidt summability of the convolution factor
   `F_g = stage3FamilyFactor g` on a fixed global basis;
2. the same-owner trace readback
   `Re Tr(F_g†F_g) = qw(g)`.

The existing `stage3Remainder_family_for_g` then supplies a positive-pair
limit family, which is consumed by the exact P2 bilateral-profile aggregate
socket and the healthy B5 `SourceRH` contradiction.

The focused owner/audit build completed successfully in 3769 jobs, with no
`error:` lines or `sorryAx`; the audit reports only
`[propext, Classical.choice, Quot.sound]`.

## Route meaning

This identifies the remaining Stage-3 work without weakening the owner or
quantifier.  FRONTIER-HS and FRONTIER-CRUX remain unproved analytic inputs;
P2/RH remain open.
