# Record 1997 — desk: the F2 un-gating bridge (owner-transport interface)

Date: 2026-09-26.

Status: desk record. No Lean brick, no rig, no theorem proved, no RH
claim. This prices the interface whose absence gates F2 (record 1993
section 4) and fixes the honest order of the next steps.

## The gate (committed, binding)

Record 1993 section 4: F2 (the order-n IBP consumer) is NOT an approved
active next step. The Gevrey-window estimate has no established
owner-preserving interface to the selected healthy CompactLog detector
or its finite visible-prime set; until that bridge is proved, F2 is a
disconnected side campaign.

## Source inventory (all committed, standard axioms)

The window family `phi_k = gevreyInner k` / `gevreyWindow k a`:

- F1a monomial algebra: `mono_invariant`, `sumAbs_le` (5^n n!),
  `Afunc` three-ratio machine `Afunc_step` (n + 16n^2/k), envelope
  `Afunc_one = 6/5` (record 1991).
- F1b identification: `deriv_iterate_gevreyInner`
  (`deriv^[n] phi_k = e^{-k/s} B_n`), `hasDerivAt_expBsum_step`
  (record 1992).
- F1c shape + glue: `abs_deriv_iterate_le_sq`
  (`|deriv^[n] phi_k u| <= sqConst n k * (1-u^2)^2`), exterior zero +
  boundary differentiability `hasDerivAt_iterate_gevreyInner_ext`,
  integrability `intervalIntegrable_iterate_gevreyInner` (record 1993).
- Vertical decay rungs 1-3: 1/||w||^{1,2,3} with explicit constants
  (records 1986-1988).
- F2 headline target (record 1990 section 4, NOT yet built):
  `|L_phi(w)| <= e^{|Re w|} (2 * Afunc n k + Mfunc n k) / ||w||^n`
  with `Mfunc` the two-ring middle bound.

## Target inventory (the consumer side)

The completed owner at the registered GO_CANDIDATE point
`rho = 1/2 + 0.10 + i*gamma_1` (records 1981/1980): owner nodes M = 12
(committed convention; M = 13 with the 6-ordinate kill book), density
`W = |L_base|^{2(N+1)} * |L_corr|^2`, finite visible-prime set from the
support radius. The Cut-1 joint margin at the witness coefficient
`lam = diagNegWitness = 1` needs `M_n <~ 7.5e-13` (window factor
~7.5e+09; geometric decay reaches it near n ~ 50; existence of such n
is FORMAL, records 1933/1935). At `lam+ ~ 70`: `M_n <~ 7.7e-09`
(n ~ 35-40).

## The gap, precisely

The owner's density window is NOT a member of the Gevrey family: the
owner is built from orbit nodes, kill widths, and amplitudes
(r80/r81 machinery); its window components are pool-selected, not
`gevreyWindow k a` for any explicit `(k, a)`. The F2 consumer bounds
`L_phi` for family members only. Three bridge shapes exist:

```text
(i)  MEMBERSHIP: exhibit the owner window as gevreyWindow k a.
     Likely FALSE — different window class (kill pools, not
     exp(-k/(1-u^2))). Not pursued without a rig witness first.

(ii) DOMINATION (the real bridge): a comparison theorem
         |L_owner(iT)| <= C(k,a) * (Afunc n k + Mfunc n k) / T^n
     for T >= T_0, with explicit C, T_0, and the owner's (k, a)
     read off its committed construction. Owner-preserving: same
     owner, same support, same prime set.

(iii) RE-TYPING Cut-1 to consume the family bound directly.
     Forbidden — changes the obligation (1993's point).
```

## Pricing (ii): what must exist before the theorem

1. A rig measurement: does the completed owner's Laplace/tail
   functional actually decay at the family's Gevrey rate (k ~ 30) on
   T >= T_0? Obstacle on record: record 1985's `cert_ok = FALSE` — the
   [8,24] annulus is the quadrature ALIASING floor of W (1.27e18), and
   below-resolution tails cannot be measured without the resolution
   certificate. So the bridge's first brick is the SAME brick as the
   1981 live-program item 1: the interval-certification resolution
   certificate for W.
2. The derivative ladder for the OWNER window (not the family): rungs
   1-3 exist for `gevreyInner`; the domination comparison needs at
   least rung-1-type control of the owner's window derivatives with
   explicit constants. The kill-pool windows are C^infinity with
   compact support, so the mechanism exists; the constants are
   unpriced.
3. The comparison theorem itself: one Lean brick consuming (1)+(2)
   plus the F2 headline once F2 exists.

## Honest order (fixes the sequencing error in my earlier summary)

My previous session summary listed "F2 consumer" as the next mainline
step. That was WRONG under the binding route map: F2 is gated. The
correct order after this desk:

```text
1  resolution certificate for W (1985 root cause; also 1981 live
   program item 1) — unblocks BOTH the D interval certification and
   any bridge measurement;
2  owner-window derivative ladder + rig domination measurement
   (this desk, item 2 of pricing (ii));
3  F2 consumer + the domination comparison theorem (un-gated only
   now);
4  witness economics recompute at T_need ~ 31.88 with LEAN constants;
5  re-bracket of the 1981 GO_CANDIDATE.
```

No gate sign is proved here; F2 remains gated until (1)-(2) exist;
RH is not claimed.
