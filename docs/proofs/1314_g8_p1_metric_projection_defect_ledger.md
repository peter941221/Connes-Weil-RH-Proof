# G8 P1 metric projection-defect ledger

Date: 2026-09-11.

The import-facing leaf `C1G8P1MetricProjectionDefectLedger` cancels the
existing same-owner internal correction from the two physical-endpoint
identities. It proves that the raw G8 cutoff trace product is exactly the
literal metric cutoff plus the three source-projection-defect channels
(projected--complement, complement--projected, and complement--complement).
No sign or vanishing of those channels is asserted.

This is FORMAL P1 ledger alignment and gives the exact finite-trace comparison
target. Metric-to-radial transport, endpoint production, the P2 remainder/sign,
and P3 remain open.

Evidence: `1494_g8_p1_metric_projection_defect_ledger.log`, owning and audit
targets green (3926 jobs), zero `error:`/`sorryAx`; the audited declaration
uses exactly `[propext, Classical.choice, Quot.sound]`.

Cross-import evidence: `1495_g8_p1_p2_p3_cross_import_defect.log` rebuilt this
ledger with the projection factorization, diagonal P1, canonical P2, and
same-detector P3 leaves (3981 jobs), again with zero `error:`/`sorryAx`.

RH is not claimed.
