# 1232 - NM Beat 0: two-channel internal-counterterm generation card

Date: 2026-09-09.

Status: `NEEDS-ANALYSIS`.  This is a paper-only Generation Card and
adversarial screen.  It opens no Lean declaration, numerical run, endpoint
claim, or RH claim.

Authority: map record [`006`](../map/006_new_math_creation_workflow.md), Beat
0.  Its sole consumer is the binding healthy-`CompactLog`, selected-detector,
same-owner B5 exit of map record [`003`](../map/003_b1_b5_minimal_exit_route_selection.md):
the remaining proposition is `orbitWindowSemiLocalGate g`, hence `0 <= qw g`
for the same healthy orbit detector.  It does not reopen B1, Line B, the
normalized/additive owner, a ROOT-to-orbit lift, or the closed fixed-rank
numerical ladder.

## 1. Generation Card G6

```text
candidate/id  G6: two-channel internal-counterterm factorization

TARGET EQUATION
  For each healthy triple-vanishing CompactLogTest g, construct a moving
  finite-part operator family Ptilde_n(g) with
      Re Tr(Ptilde_n(g)) >= 0,
      Re Tr(Ptilde_n(g)) -> qw(g).
  This is the L4 projection-cutoff contract; its already-landed aggregate
  readback is the A4 form of the same P2 conclusion.

NOVEL MOVE
  The compensating finite part is part of the operator kernel/sandwich at
  every cutoff, not a scalar subtracted after taking a positive trace.  It
  must retain the two geometric channels detected in record 1224:
  the P_r(lambda) sandwich and the P_V(S) shadow, with their actual
  non-separable lambda/S response.

SIGN SOURCE
  A literal positive factorization at every cutoff, for example
      Ptilde_n(g) = Atilde_n(g)^* Ktilde_n(g) Atilde_n(g),
  or a proved equivalent positive block-kernel factorization.  A limit of
  these nonnegative real traces supplies the sign; no main-term-versus-tail
  comparison is allowed.

CHEAP FALSIFIER
  On the named two-channel finite-cutoff owner, the proposed correction must
  (i) occur before trace formation, (ii) reproduce both channel contributions
  in an exact finite-cutoff identity, and (iii) not factor through only finite
  rho/Mellin node values.  External subtraction, a rank-one/affine response,
  or a node-only profile bridge kills that proposed formula before Lean work.
```

## 2. Non-negotiable screen obligations

```text
G6.1  Name the actual analytic Hilbert space, P_r(lambda), P_V(S), and the
      moving response/counterterm in one same-owner formula.  A MODEL matrix
      is not such a formula.

G6.2  Prove a finite-cutoff trace identity whose right side includes the
      Archimedean and actual finite visible-prime contributions of the same g.
      The identity must precede any limiting argument.

G6.3  Prove positivity of the corrected kernel itself.  A displayed equality
      "positive trace minus B_n" fails this condition even if B_n is known.

G6.4  Establish a genuine physical-profile bridge.  It must use more than the
      finite raw Laplace-node equations and survive the orbit construction,
      convolution square, half-density convention, and selected prime owner.

G6.5  Prove convergence/readback to qw(g), not merely boundedness, operator
      norm decay, or a fitted finite part.

G6.6  Pass the target-satisfiability audit without storing same-g positivity
      in data or assuming SourceRH, qw(g) >= 0, or an equivalent direct gate.
```

## 3. Adversarial screen

Evidence level: FORMAL interface audit plus MODEL constraint, with no new
numeric evidence.

* G1 already formally rejects the universal negative gate-matrix/Gram version:
  a negative-semidefinite gate matrix on a span containing a healthy detector
  would itself imply the refuted direct gate sign (record 1229).
* G5 formally proves that finite raw Mellin-node equations do not determine a
  physical value.  Therefore node-only off-diagonal blocks cannot meet G6.4
  (record 1231).
* The exact orbit construction currently exposes corrections only through
  nested existentials and exports no affine/canonical physical-profile family;
  G4 therefore cannot yet even state the required bridge (record 1230).
* The record-1224 two-channel/RANK-3 finding is MODEL evidence only.  It rules
  out rank-one, affine, and separable *realizations of that model response*;
  it does not prove an analytic no-go.  It does, however, make such a response
  ineligible as G6's claimed factorization.

Verdict: `NEEDS-ANALYSIS`, before any Lean or prototype work.  No current
declaration supplies G6.1 or G6.4: there is neither a concrete analytic
two-channel internal kernel nor a legal physical-profile map for the selected
orbit construction.  This is an ownership/API stop, not a death verdict for a
future formula with genuinely additional same-owner structure.

## 4. Exact next admissible work

The source-family part of the previously requested bridge has now landed in
record [`1234`](1234_affine_mellin_correction_family_preregistration.md): the
finite-Mellin map is surjective and has a linear right inverse with exact
support/node readback.  This does **not** determine the physical profile after
convolution-square formation, so G6.4 is still open.

The next paper brick is therefore narrower and genuinely analytic: construct
one semilocal prolate/Sonin owner (or an equivalent positive block kernel)
whose two channels are the same selected detector's finite-visible-prime
shadow and archimedean support projection.  It must carry the internal
correction, a finite-cutoff positive trace identity, and the `qw` limit
readback together.  Record [`1238`](1238_semilocal_prolate_translation_gap.md)
closes the direct literature-import path and lists the exact five readbacks.
No numerical campaign is authorized by this record.

RH is not claimed.
