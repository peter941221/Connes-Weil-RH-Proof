# 114 Fixed-Detector Route Executable Rejection

Date: 2026-07-12

## Result

Plan 028 is rejected as an executable RH route.  Its arithmetic owner and
classical spectral formula are valid, but every currently named positive-trace
consumer fails before route Lean is justified.

## Evidence Chain

```text
107/109  same-square arithmetic owner and Burnol normalization pass
113      prime-free M4 complement is already strictly QW-positive
042      endpoint metric projection doubles the p^2 Weil atom
110      exact compact Xi-null corrections are impossible by zero density
111      positive log-Poisson square-root trace loses the linear Weil read-off
112      CCM24 proves Sonin transport, not the finite-S positivity theorem
```

The remaining statement is not an API bridge:

```text
one same-object finite-S positive trace
  = full selected QW
    + post-Q (-2 Id + compact remainder),
```

with every `p^m` translation removed from the compact remainder at the exact
Euler logarithm coefficient.  No checked source or current construction proves
this.

## Scope

This verdict does not prove that no semilocal trace proof of RH can exist.  It
rejects the currently executable mechanisms and prevents a source gap from
being encoded as a Lean premise.  A genuinely new trace identity satisfying
Plan 036 may reopen the route.

```text
Plan 028 executable status: rejected
Burnol explicit formula: retained as valid infrastructure
finite-S same-object producer: absent
Lean route work: denied
RH: unproved
```

