# 1576 — the phase wave's typed stop is filed; AO re-priced at the correct level (it cannot feed (★)); the surviving class is support-combinatorial (B3, priced by 1575)

Date: 2026-09-17.

Status: PAPER FILING + STRUCTURAL RE-PRICING. Zero Lean, zero digits. This
record executes the last two obligations of the 1568 pre-registration: the
typed stop promised by its section 3 ("If T1 closes with `beta <= 1` for
every symbol decay the committed carrier allows, the typed stop fires and
the finding is recorded as a TYPED impossibility for multiplier-phase routes
into (★) - which is itself a deliverable") and the re-pricing ordered by its
section 5 ("If T1 kills multiplier-phase routes, the wave records the
exponent proof and re-prices (★) from the remaining routes (AO) before any
further spend"). RH not claimed.

## 1. The pre-registered condition, verbatim, and why it never fired

```text
1568 §4.3: "T2 (only if T1 > 1): Cotlar almost-orthogonality across the
           Sonin-scale family with phase-derived constants; this is what
           feeds (★)."
1571 verdict: beta_+ = 5/2 > 1 (positive ray, closed) BUT
              beta_- = 1/2 < 1 on the negative ray FOR ANY symbol decay k.
1572 verdict: the third hoped source (projection modulation) is TYPED DEAD
              (sharp indicator; edge gain <= 1/log|xi| by T0a, never a power).
```

The admission condition for T2 is a CONJUNCTION over both rays; it failed at
1571 and the one loophole (modulation) was closed at 1572. The wave therefore
owes no T2 attempt - it owes the FILING. Filed (scope guards in §1.1):

**TYPED STOP.** No estimate for the (★) gate or the B4 gap column
`D E H (I-E) H E M J` exists inside the pre-registered lever class:

```text
  class = { single-vdc/T0b exponent 1/2, root-symbol decay k on the dual
            scale, modulation/symbol reading of E (1572), prolate
            absorption as subtraction only (1506/1531/1532), exact hM
            identity (1575: fixed-scale FAILS), AO across a tail family (§3) }
```

applied through the translation-invariant screening model of 1571 on the
two-sided carrier. By 1572 section 1 (`Q = H E H`), (★) and the B4 gap
column are ONE family, so the stop covers both simultaneously.

### 1.1 Scope guards (what the stop is NOT)

- It is NOT `not (★)`: the gate may hold by a mechanism outside the class
  (or the model may misprice a structured input); the stop prices ROUTES.
- The class is the PRE-REGISTERED one; a genuinely new lever class must
  enter through the map-010 admission gates (bone-foundry rule: each gets
  a typed verdict, positive or negative).
- T0a/T0b (1570) stay paid and reusable: they are phase FACTS, not routes.

## 2. Why AO cannot feed (★): the level mismatch

The gate demands HILBERT-SCHMIDT square-summability
`Summable i, ‖P C J e_i‖² = Tr(T†T)` (`= ∫∫|K|²` on the carrier kernel).
The 1503 AO route as written ("cross-structure summable ... bounded by the
Cotlar-Knapp argument") operates at the OPERATOR-NORM level: classical
Cotlar-Stein consumes bounds `‖Q_M† Q_N‖_op <= a(M-N)` and returns
`‖Σ Q_N‖_op <= Σ a`. Operator norm bounds do NOT bound the trace of
cross pieces: `|Tr(Q_M† Q_N)| <= rank · ‖·‖_op` with no rank control here.
Feeding an HS gate needs the strictly stronger NUCLEAR almost-orthogonality
`‖Q_M† Q_N‖_tr <= a(M-N)` - which is not what was preregistered and is not
what phase stationary phase gives (T0b sizes oscillatory integrals, i.e.
kernels/norms, not trace norms).

## 3. What AO degenerates to, in either decomposition choice

For completeness the two ways a tail family `Q_N` can be built are both
checked against the HS identity `‖T‖²_HS = Σ_{M,N} ∫∫_{A_M×A_N}|K|²`:

1. **Frequency/spectral annuli** (disjoint multiplier supports): cross terms
   `Q_M† Q_N = M_{φ̄_M φ_N} = 0` EXACTLY for `M ≠ N`. AO collapses to
   PYTHAGORAS: `‖Σ Q_N‖²_HS = Σ ‖Q_N‖²_HS` - summability of the left IS
   summability of the right, and each annulus re-presents the same screened
   exponent problem (the 1571 ledger is scale-covariant: β is a ray
   exponent, annulus-by-annulus divergence at β <= 1 unchanged). NO new
   content.
2. **Support annuli** `A_M` of the carrier: cross blocks are exactly the
   off-diagonal integrals over `A_M × A_N`; summing them over `M ≠ N` is
   summing the off-diagonal mass of `|K|²`, which the negative-ray ledger
   already priced DIVERGENT at `β_- = 1/2`. A summable `a(M-N)` envelope
   would force absolute summability of the same quantity - circular, not
   supplemental.

Verdict: **AO is not a rescue for (★) or the B4 gap column - it is either
trivial (exact Pythagoras) or a renaming of the failed off-diagonal sum.**
1568's sequencing ("T2 only if T1 > 1") was correct; its phrase "this is
what feeds (★)" is superseded by this record (registered as a law-F21-level
lesson: an AO hope at the wrong LEVEL was not caught by the exponent ledger
because it is a TYPE question, not a cost question - level checks belong in
preregs next to F21).

## 4. What the re-pricing leaves alive

> **AMENDED 2026-09-17 by [1577](1577_b3_composite_phantom_verdict_and_card_repricing.md)
> and [1578](1578_w0_archimedean_symbol_pinned_and_window_class_correction.md).**
> Item 1 below is a PHANTOM: B3's radial OUT leg was already proven
> unconditionally for both physical boundary columns at
> `C1G8R3CompositeBoundaryEnergy.lean:1147-1200` (records 1555/1558/1560/1561),
> with the wide-radial support discharged internally at the one-step shift
> `Real.log p`; no committed consumer awaits a `Lambda_max` composite window.
> 1575's `Lambda(p::S)` belongs to B4's `hradial` strip leg, not to B3. Item 2
> (`hradial`) is real but CAPPED: the B4 consumer needs `hradial` AND `hgap`
> together (`C1G8R3BoundaryOutputFactorizationBridge.lean:509-527`), and `hgap`
> is the very column this record's stop covers - so closing `hradial` closes
> nothing on its own. The decision card at §5 is decided there; option (iv) was
> owner-selected and is executed in 1578. Text retained unmodified for the
> pre-registration trail.
>
> FINAL ADJUDICATION [1579](1579_g8_survivors_adjudicated_hradial_priced_and_support_lemma_committed.md),
> 2026-09-17: item 1's "NOT committed yet" support-propagation lemma was
> already committed at `CC20YoshidaConvolution.lean:386`
> (`convolution_support_subset_add_Ioo`, plus the shape-free
> `MeasureTheory.support_convolution_subset` beneath it), and the 1495 pattern
> needed no shifted-union check because it is an exact translation conjugation
> - item 1's prereq was itself doubly phantom. Item 2 is now PRICED CLOSED:
> both committed radial/gap summability generics hardwire `rootConvolution`
> and draw all compactness from it (law F29: support is location, summability
> is volume), and the physical B4 chain has no kernel slot - so the support-
> combinatorial route this item advertised cannot reach it, independently of
> the 1577(ii) cap. The §4 survivor queue is exhausted at paper level.

The exhaustion is of the PHASE-EXPONENT class. Two non-phase classes remain,
one of them made concrete by the 1575 ledger:

1. **B3 composite window (support-combinatorial; prereg 1503 §4, "cheap").**
   The outer boundary leg `(I-E) C M_p J` never applies `H` - its control is
   support arithmetic. Committed data: `rootConvolution owner =
   cc20GlobalLogConvolution owner.sourceTest.involution.test`
   (`CCM24FiniteSBandTrace.lean:36-39`), kernel support = the flipped bundle
   `hsupp : support test ⊆ Icc a c`; 1575 supplies the shift set in closed
   form `Lambda(p::S) = log prod_{q in p::S} q`. Together they place the
   whole leg inside the FIXED compact window `[tau - Lambda_max - R, tau)`
   - as an identity WAITING for its cheap pointwise support-propagation
   lemma (convolution with kernel in `[-R', R']` moves a half-line boundary
   left by at most `R'`: standard and short, but NOT committed yet - exactly
   the "algebraic support calculus formalizable cheaply" the prereg names),
   and record 1496's analytic transfer applies to a compact window
   unchanged. Prereg says: "the composite window identity as the record-1495
   identity with the shift-set union; the algebraic support calculus is
   formalizable cheaply". So the composite-window identity is now writable
   WITHOUT any phase estimate. NEXT PAPER STEP: verify the 1495
   pattern accepts a shifted half-line union (it was stated for compact
   `[-R,R]`-type windows; `[tau - Lambda, tau)` is compact - check the
   measurability/coframe plumbing) and then a Lean brick.
2. **hradial as a strip object (estimate, not phase).** By 1575 §3.3 the
   radial defect is `D o 1_[tau-Lambda, tau) o M_p J N`; its square-sum is
   a new named object (HS-ness of a strip-restricted composition), outside
   the phase class, outside the AO class - UNPRICED (no verdict claimed here;
   it needs its own screening computation before any spend).
3. **The prime-aggregation AO (kept alive, renamed).** The one place an
   almost-orthogonality statement is still well-posed is NOT (★) but the
   visible-prime sum inside `hBoundary`/rho5 (`‖Σ_p T_p e_i‖²` cross
   `Tr(T_q† T_p)` terms) - and there it must be NUCLEAR-level (§2), with
   1575's shift calculus as the candidate geometric input. No paper name,
   no constants: registered, not scheduled.

## 5. Boundary and decision content

- MOVED: the 1568 wave's stop obligation is discharged with its typed
  reason; AO re-priced and retired as a (★) feeder; B3 upgraded from "cheap"
  to "explicit window in hand" (1575's Lambda); hradial and the
  prime-aggregation AO registered with their correct levels.
- NOT MOVED: (★), B4, rho5, transport, R4 all OPEN; no mathematical negation
  of any gate; T0 facts retained.
- OWNER DECISION CARD (the queue on the (★)/B4 phase face is empty):
  (i) B3 paper-verify then brick (cheap, closes one diagonal leg);
  (ii) hradial strip screening (new paper computation);
  (iii) rho5 combined-row face (independent of this wave);
  (iv) map 011 track W face (the 1417 category-change line).
  RH not claimed.
