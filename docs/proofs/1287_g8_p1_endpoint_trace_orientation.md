# G8 P1 endpoint trace orientation (2026-09-10)

The endpoint orientation bridge now also has an ordinary-trace form on any
named source Hilbert basis:

```text
Tr(L† W_g J) = − star(Tr(sourceBandGramResponse)).
```

This follows from the same-owner operator identity in record 1286, linearity
of the diagonal-series trace under negation, and the existing adjoint trace
identity. It is a scalar orientation/readback handoff only; it does not prove
the finite prime-power value, a cutoff limit, an A4/P2 inequality, or P3.

Acceptance: `/home/peter/rh/build-logs/1314_g8_p1_endpoint_trace_orientation.log`,
3926 jobs, zero `error:`/`sorryAx`, and standard audit axioms only.
