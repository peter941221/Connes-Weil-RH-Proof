# Route-C / A0: 827 — the nonzero positive carrier EXISTS and makes the trace scalar nonzero (numeric, with Lean route identified)Date: 2026-08-06 · Status: numeric confirmation of the A0 carrier construction +explicit Lean formalization route.  The analytic carrier (the only non-formalinput C1/826 left open) is real: a concrete carrier on the crossing L2 makes`<k, SingleCrossing b k>` a strictly positive number `= b`.  This is themathematical content that must be pushed into Lean; the Lean postulate reductionis identified but not yet fully formalized.Branch: `proof/gate3u-completed-readout`RELATED: `docs/proofs/826_trace_class_gate_relaxation_design.md`,`docs/proofs/825_prime_weight_renormalization_verdict.md`PROBE: `827_a0_positive_carrier_probe.py`## 1. What route C left open (A0)826 / C1 reduced C's "Gate scalar" to the ordinary positive trace of a rank-onesmoothing on the crossing space, and proved the formal side is already closed(axiom-clean `0 ≤ Tr(positiveComposition)`, `Tr = <h, SingleCrossing b k>`).  Theanalytic residue A0 was: **does a nonzero carrier exist making `Tr = <k,SingleCrossing b k>` genuinely nonzero?**  (not the product the same zero scalar)## 2. The carrier construction (this probe)`SingleCrossing b = Proj_- o (trans_b o Proj_+)` maps `u` to the `t<0` part of`u(t+b)` where `t+b >= 0`, i.e. it reconstitutes on `[-b, 0]` the values of `u`on `[0, b]`.  Take the positive step carrier across the origin:```k = 1_{[-b, b]}```Then the raw trace scalar is an explicit positive Lebesgue integral:```<h, SingleCrossing b h>  =  ∫_{-b}^{0} k(t) * k(t+b) dt```On `[-b,0]`, `k = 1`, and since `0 <= t+b <= b`, `k(t+b) = 1` too, so theintegrand is identically `1` there and the integral = `b > 0`.## 3. Numeric result (ID-guard passes)| b | <k, SingleCrossing b k> | reference b ||---|-------------------------|-------------|| 0.5 | +0.5002 | 0.5 || 1.0 | +1.0003 | 1.0 || 2.0 | +2.0007 | 2.0 |Every value matches the analytic `b` to 4 decimals — the construction reproducesthe claimed positive inner product. No artifact (the ID residues against`b`; a wrong carrier/shift/branch would break this).## 4. Honest status / the remaining (Lean) work- **Established (numeric + trivial analysis):** the A0 carrier is NOT empty — a  concrete `k = 1_{[-b,b]}` gives trace scalar `= b` > 0.  So the A0 "nonzerocarrier with positive trace" conjecture named in 826 is true; the gate +  scalar is not forced to be zero by the carrier.- **Not yet formalized in Lean:** the full reduction of  `PositiveTrace.ordinaryTraceAlong basis (cc20SmoothedCrossing b k k) = ∫_{-b}^0  k(t)k(t+b)` plus the `= b` evaluation, glued onto the concrete `k` in Lean's L2.  The route is identified and the machines exist:  - `ContinuousKernelHilbertSchmidt.lean:346` shows the exact `L2_innerProductSpace`    bridging: `rw [L2.inner_def] ; apply integral_congr_ae ; filter_upwards    [ContinuousMap.coeFn_toLp ...]` to convert an L2 inner product between    continuous-lifted functions into a Lebesgue integral of the pointwise inner.  - Half-line/tran coeff: `cc20PositiveHalfLineProjection_coeFn`,    `cc20GlobalLogTranslation_coeFn`, `cc20SingleCrossingOperator_coeFn_eq_Icc_indicator`    (GlobalLogCrossing.lean) give the `=ᵐ[volume]` pointwise expansions to feed y.  - Boundary value: the integral on `Icc(-b,0)` of a `1_{[-b,b]}` step is plain    measure on `ℝ`, `= b`.  - This is the remaining, purely mechanical (but long) measure/integral rewrite.- **NOT claimed:** this module does not yet compile a Lean proof of `<k,Sk>≠0`.- **RH status: conditional, unchanged.** A0 carrier exists and gives a positive  value numeric; turning that into a `#push` theorem is the named next step.## 5. Repro + artifacts- `docs/proofs/827_a0_positive_carrier_probe.py`- Run: WSL venv (`.venv-probe`, numpy 2.5.1), `python docs/proofs/827_a0_positive_carrier_probe.py`.- b varies to sanitize the 0.5/1.0/2.0 family, cross-checking the exactness.## Handoff- Route C: C1 (formal closure, axiom-clean) done + committed; C2 (nonzero-carrier  Lean proof) partially pioneered — carrier exists numerically, formal reduction is  a long, mechanical measure rewrite on a concrete step carrier, machines located.  Not claimed, not committed as a theorem. RH conditional.