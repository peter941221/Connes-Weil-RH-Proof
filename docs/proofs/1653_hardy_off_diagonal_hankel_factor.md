# 1653 - Off-diagonal Hankel factor of the S3 Hardy defect

Date: 2026-09-19.

Status: formal exact positive-factor identity; the required column estimate
remains open. RH is not claimed.

For `P` the positive-half-line projection, `M = I-P`, and `K_b` the shifted
Hardy involution, the P-side S3 defect now has the exact form

`P - P K_b P K_b P = (M K_b P)^* (M K_b P)`.

Thus the remaining analytic target is the first-order off-diagonal Hankel
factor `M K_b P`, not the already controlled interior compression `M K_b M`.
The distinction is structural: the former is the cross-block whose positive
square is the live defect, while the latter is a different block.

The paired Audit prints only the standard three axioms. No trace-class upgrade,
uniform angle, detector sign, or RH premise is used.
