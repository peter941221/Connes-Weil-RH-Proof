# 1536 — Fourier-gap defect as a Hardy radial tail

The source Hardy column now has an exact second normal form.  For every
ambient factor `M`,

`E (I-Q) E M J = E H (I-E) H E M J`,

where `H` is the genuine Hardy–Titchmarsh involution, `E` is the radial
projection, and `Q = H E H` is the Fourier-support projection.  The proof uses
only the involution law and keeps `M` inside the column; no projection
commutation is assumed.

This identifies the remaining Fourier-gap producer with a radial tail after
Hardy scattering.  It is the form needed to connect the formal consumer to a
quantitative Hardy–Titchmarsh phase estimate.  Evidence is the paired audit
build in log `/home/peter/rh/build-logs/1742_hardy_fourier_gap_conjugation.log`
with zero `error:` and `sorryAx` lines.  No tail estimate or B4/RH closure is
claimed.
