# Record 1932: four-point span route priority and first attack

Date: 2026-09-24.

## Route decision

Route A remains logically valid: a negative `ICgate` for the actual healthy
owner would contradict the already formal positive gate orientation and would
therefore be the RH producer itself. It is not a dead route, but it is not an
independent preparatory witness-construction round.

The primary attack is therefore the same-span four-point route of map 103. Its
current formal base is stronger: the exact parabola and positive-vertex
witness, finite spectral-prefix bound, and weighted high-shell transport are
already formal. Its remaining gate sign is one aggregate determinant on the
same span.

This changes execution priority only. It does not change the binding B5 route,
the owner, or the RH claim status.

## First attack target

For the selected four-point span, prove

```text
D * C - B01^2 < 0
```

with `D = ICgate(u.square)`, `C = ICgate(g.square)`, and `B01` the symmetric
cross entry, together with `B01 > 0` and `C > 0`. The existing formal vertex
lemma then supplies a positive coefficient with strictly negative span gate.

Records 1919 and 1920 reduce this to the explicit signed-kernel moment
bracket. The first subattack is an aggregate bound for the visible-prime
kernel and its archimedean cross term on the actual owner; a prime-only sign
without cross domination is insufficient.

## Stop condition

This campaign advances only if it lands an explicit parameterized determinant
margin, a named counterexample/no-go, or a strictly smaller exact inequality
with at least one old analytic premise discharged. Numerical scans alone do
not change the map.

Classification: route decision FORMAL/PROJECT PLAN; first determinant
certificate OPEN. RH is not claimed.

## First-strike audit

The existing formal polarization reduces the cross-sum sign exactly to the
ordering of two same-owner evaluations:

```text
2 * (B01 + B10) = Q(-1) - Q(1).
```

No current theorem orders `Q(-1)` and `Q(1)` for the actual four-point owner.
Thus the first strike does not have a hidden algebraic shortcut: the minimum
new analytic obligations are the sign of this polarized gap and the strict
determinant bound. The carrier polarization theorem cannot be reused as a
sign theorem; it is an identity only.
