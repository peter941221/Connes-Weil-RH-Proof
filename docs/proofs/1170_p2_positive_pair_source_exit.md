# Record 1170 — P2 positive-pair `SourceRH` exit

Date: 2026-09-06

## Result

`sourceRH_of_healthyDetector_p2PositiveTracePairLimitFamily` takes fixed
Hilbert spaces and basis and, for every right-oriented off-line zero, a
same-owner healthy detector with a self-pair positive-trace limit family.  The
existing pair-trace positivity theorem yields `qw ≥ 0`; the exact bilateral
profile aggregate consumer then supplies the healthy B5 contradiction.

The focused owner/audit build completed successfully in 3721 jobs, with no
`error:` lines or `sorryAx`; the audit reports only
`[propext, Classical.choice, Quot.sound]`.

## Route meaning

The pair and general positive-operator trace routes now have symmetric formal
`SourceRH` exits on the same owner.  Constructing either producer for the
pinned orbit remains the open P2 content; RH is not claimed.
