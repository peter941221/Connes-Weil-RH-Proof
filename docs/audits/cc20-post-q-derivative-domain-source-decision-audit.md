# CC20 Post-Q Derivative Domain Source Decision Audit

Status: source-import decision audit for
`PostQDerivativeDomainCompatibility`.

This audit refines:

```text
docs/proofs/cc20-post-q-derivative-domain-compatibility-theorem-contract.md
docs/audits/source-import-legitimacy-audit.md
docs/audits/sign-defect-blocker-audit.md
```

It asks a narrower question:

```text
Can PostQDerivativeDomainCompatibility be discharged by importing a CC20 or
CCM24 source theorem as-is?
```

## Result

Good result:

```text
The project has a plausible internal proof route through the existing
fixed-S graph/Cdef calculus.
```

Bad result for source import:

```text
No CC20 or CCM24 source theorem currently found discharges the fixed-S
derivative-domain transport of the CC20 post-Q bulk term.
```

Decision:

```text
PostQDerivativeDomainCompatibility is project-proof-required.
It is not source-import-discharged.
```

## Source Evidence

### CC20

| lines | evidence |
|---|---|
| `weil-compo.tex:1199-1201` | CC20 starts analyzing `E circ Q` through coefficients of the scaling representation and defines `D_u` |
| `weil-compo.tex:1260-1264` | the formal `D_u` identity cannot be applied directly because `xi_n,zeta_n` are not in `Dom(D_u)` |
| `weil-compo.tex:1267-1270` | Lemma `devil3` replaces the failed formal identity by the bulk-plus-boundary formula |
| `weil-compo.tex:2171-2211` | CC20 bounds the bulk contribution `A_n(rho)` using `D_u xi_n` and `D_u zeta_n` estimates |

What CC20 gives:

```text
source-owned archimedean bulk formula
source-owned warning about the D_u domain failure
source scalar/L2 estimates for the derivative factors
```

What CC20 does not give:

```text
fixed-S graph representatives for D_u xi_n and D_u zeta_n
transport through V_S=M_S U_S
equality between the transported fixed-S bulk object and the CC20 source bulk
```

### CCM24

| lines | evidence |
|---|---|
| `mainc2m24fine.tex:805-823` | `iota_S` is bounded with bounded inverse between the two spectral `L2` models |
| `mainc2m24fine.tex:846-852` | `N_S eta_S != eta_S N_infty` and `|.|_S^2 eta_S(f) != eta_S(|.|^2 f)` outside the archimedean case |
| `mainc2m24fine.tex:1022-1029` | `theta_S` is a Hilbert-space isomorphism of Sonin spaces |

What CCM24 gives:

```text
fixed-S Hilbert model
bounded L2 comparison
Sonin-space isomorphism
```

What CCM24 does not give:

```text
automatic graph-domain transport for CC20's D_u factors
automatic preservation of the archimedean differential expression
automatic conversion of source derivative terms into route Cdef terms
```

## Project Route Evidence

The repository already has route-evidence packages that point toward a
project proof, but they are not source imports.

| file and lines | evidence |
|---|---|
| `docs/proofs/fixed-test-graph-cdef-exhaustion.md:73-85` | `Cdef_graph` is a finite sum of graph seminorms generated by support/Fourier tails and commutators with `M_S`, `M_S^*` |
| `docs/proofs/fixed-test-graph-cdef-exhaustion.md:112-141` | fixed finite-S multipliers are bounded on fixed graph domains and preserve Schwartz-tail decay up to fixed polynomial weights |
| `docs/proofs/rank-repair-finite-normal-form.md:226-236` | the project route records `[D_u,T_a]=0` for finite-prime shifts in logarithmic coordinates |
| `docs/proofs/semilocal-q-compact-form.md:279-289` | applying `Q` only raises graph order and endpoint evaluations are controlled by finite-strip Sobolev estimates |

This evidence suggests the right proof strategy:

```text
source D_u term
        |
        v
log-coordinate graph derivative
        |
        v
fixed finite-S Euler/shift graph-bounded transport
        |
        v
same fixed-S route coordinate V_S
        |
        v
input for Row 4 projection-defect classification
```

Before the proof package, the route evidence still lacked the final
source-equality bridge:

```text
fixed-S graph object
  =
transported CC20 source bulk object.
```

## Decision Table

| requirement | evidence status | decision |
|---|---|---|
| source bulk formula exists | CC20 Lemma `devil3`, `weil-compo.tex:1267-1270` | passed |
| source domain failure visible | CC20 `weil-compo.tex:1260-1264` | passed |
| source derivative estimates exist | CC20 appendix `weil-compo.tex:2171-2211` | passed |
| CCM24 fixed-S Hilbert transport exists | CCM24 `mainc2m24fine.tex:805-823`, `1022-1029` | passed at L2/Sonin level |
| CCM24 graph-domain transport for `D_u` exists | not found; source warns against naive operator transport | not importable |
| project graph route exists | route-evidence packages above | plausible project proof route |
| source-to-route bulk equality exists | route-evidence proof package now supplies it; not source-import or Lean discharged | project-level only |

## Required Project Proof Bridge

The next proof target should be:

```text
FixedSPostQBulkGraphTransfer(S,I,lambda,g,F_g,n):
  CC20PostQBulkDerivativeObject(n)
  + CCM24 fixed-S model/window/comparison
  + fixed-S graph calculus for finite Euler shifts
  ->
  PostQDerivativeDomainCompatibility(S,I,lambda,g,F_g,n)
  and PostQBulkFixedSEquality(S,I,lambda,g,F_g,n).
```

The theorem contract is now:

```text
docs/proofs/fixed-s-post-q-bulk-graph-transfer-theorem-contract.md
```

The route-evidence proof package is now:

```text
docs/proofs/fixed-s-post-q-bulk-graph-transfer-proof-package.md
```

Minimal proof obligations:

| obligation | purpose |
|---|---|
| source-to-log graph translation | interpret CC20 `D_u` factors in the same logarithmic graph calculus used by the route |
| fixed finite-S Euler graph boundedness | ensure `M_S`, `M_S^*`, and finite shifts preserve the required graph domain |
| derivative-shift commutation | use `[D_u,T_a]=0` only in the fixed log coordinate where it is legal |
| same tuple and window | prevent a fresh cutoff from entering the graph argument |
| source bulk equality | prove Row 4 classifies the transported CC20 bulk term, not a route-local lookalike |

## Source Import Consequence

`PostQDerivativeDomainCompatibility` must be treated as:

```text
project-proof-required
```

not:

```text
CC20 source theorem
CCM24 source theorem
source-import discharged
```

This directly strengthens the source-import audit:

```text
source line exists
  !=
source import is legitimate.
```

## Current Judgment

| question | answer |
|---|---|
| Is the CC20 post-`Q` bulk term source-owned? | yes |
| Can its fixed-S derivative-domain transport be imported from CC20/CCM24 as-is? | no |
| Is there a plausible project-proof route? | yes |
| Does this discharge Row 3? | no |
| What should be proved next? | `FixedSPostQBulkGraphTransfer` |

The fast-search status is:

```text
first Row 3 subcontract classified:
not source-importable, project-proof-required.
```
