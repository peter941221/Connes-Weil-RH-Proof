# 036 Fixed-Detector Executable Verdict

Date: 2026-07-12

Status: Plan 028 rejected as an executable route; one new-theorem reopen
contract remains.

## Gate Ledger

```text
Burnol same-square explicit formula normalization   pass
selected per-square finite-prime owner              pass
prime-free Xi detector in M4 complement             rejected (113)
endpoint metric semilocal positive trace            rejected (042)
exact compact Xi-null correction                    rejected (110)
log-Poisson positive trace shortcut                 rejected (111)
source-backed CCM24 finite-S inequality             absent (112)
```

The classical spectral identity remains worth formalizing as infrastructure,
but it cannot close RH without the missing finite-S positive producer.

## Reopen Contract

A successor must construct, on one test-root Hilbert space,

```text
H_I
xi(g) in H_I
Pos_S(g) >= 0
Pos_S(g) = QW_S(g,g) + <xi,(-2 Id+K_(S,I))xi>
K_(S,I) compact and self-adjoint
```

and prove that every noncompact single-crossing channel has already been read
as the finite-prime main term with coefficient

```text
p^(-m/2)/m
```

before the crossing length supplies `m log(p)`.  No finite Euler translation
may remain inside `K_(S,I)`.

The theorem must not use global Weil positivity, RH, detector coverage, a
stored remainder sign, or a domain transport theorem as its sign premise.

## Execution Rule

Do not formalize the Burnol contour theorem, rewire the route, or create a
finite-S Lean owner until a paper proof of the reopen contract survives the
`S={infinity,2}`, `m=2` coefficient and common-domain gates.

See proof 114.

