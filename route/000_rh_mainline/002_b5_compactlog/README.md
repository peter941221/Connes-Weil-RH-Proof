# 002 — Binding B5 CompactLog mainline

Status: BINDING MAINLINE. RH is not claimed.

Consumer:

```text
selected healthy CompactLog detector g
    -> prove qw(g) >= 0 for that same owner
    -> SourceRH
    -> Mathlib RiemannHypothesis
```

Current state:

```text
formal:
  detector construction
  strict spectral negativity
  support-derived finite visible-prime ownership
  contradiction consumer to SourceRH

open:
  detector-specific semi-local nonnegativity
```

Child routes:

- `001_route_a_signed_kernel/README.md`
- `002_route_b_fourpoint_span/README.md`
- `099_frozen_or_audit_only/README.md`

Authoritative records:

- `docs/map/003_b1_b5_minimal_exit_route_selection.md`
- `docs/map/README.md`
- `docs/map/080_c3p_signed_certificate_owner.md`
- `docs/map/082_direct_semilocal_gate_assault.md`

Rule: every alternative route must either prove the same-owner `qw(g) >= 0`
obligation or produce a reproducible scoped no-go. Generic positivity, ROOT-only
positivity, and fixed-prime surrogates do not satisfy this route.
